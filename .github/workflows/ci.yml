name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  validate:
    name: Lint, Type Check & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Generate route trees
        run: |
          npx @tanstack/router-cli@1.166.8 generate
          npx @tanstack/router-cli@1.166.8 generate --routesDirectory apps/demo/src/routes --generatedRouteTree apps/demo/src/routeTree.gen.ts

      - name: Lint
        run: npm run lint

      - name: Type Check
        run: npx tsc --noEmit

      - name: Test
        run: npm test
