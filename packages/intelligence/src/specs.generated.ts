// AUTO-GENERATED FILE — do not edit by hand.
//
// Sourced from `specs/*.md` by `scripts/generate-specs.ts`. Run
// `npm run prebuild -w @capybudget/intelligence` (or any build) to
// refresh after editing a spec.
//
// SPECS: map of filename → full file content. Consumed by the
//        `read_spec` tool, and by the chat / import / enrich prompts
//        which embed `APP_KNOWLEDGE.md` as the shared always-on brief.

export const SPECS: Readonly<Record<string, string>> = Object.freeze({
  "APP_KNOWLEDGE.md": "# Capy — working knowledge\n\n## The model\n\nEverything is a transaction. Balances, net worth, budgets, and every chart are derived from the transaction log — never stored. A clean log makes everything downstream correct; a messy one corrupts all of it. Maintaining that log is the core job.\n\n**Transactions** — three types:\n- **Expense** — outflow (negative). Reduces the account balance. Categorized.\n- **Income** — inflow (positive). Increases the balance. Categorized.\n- **Transfer** — moves money between two of the user's own accounts: two linked legs (negative on source, positive on destination). No category. A transfer is not spending and doesn't change net worth.\n- **Credit-card lifecycle** — a purchase on a card is an *expense* (card balance more negative, categorized). Paying the card down is a *transfer* from another account, not spending. Categorizing the payment as spending double-counts the purchase — a real error worth catching.\n\n**Accounts** — any store of value: `cash · checking · savings · credit_card · loan · asset · crypto`. Balance = sum of the account's transactions; never typed (an opening balance enters as a transaction). Assets are positive, debts negative. Net worth = sum of non-archived account balances; individual accounts can be excluded from it.\n\n**Currency** — each account holds one currency, set at creation (defaults to the budget default). Amounts store native and never re-rate. Roll-ups convert into the budget default: flows (spending/income) at each transaction's stamped rate, holdings (balances/net worth) at today's rate. The gap between a foreign balance's cost (flows) and spot (holdings) value is unrealized FX, shown as a net-worth FX delta. Aggregates from `group_transactions` and `list_accounts` balances are already in the default; `list_transactions` rows show each account's own currency. A single-currency budget has no FX anywhere.\n\n**Categories** — grouped by controllability, and the grouping carries the reasoning, not just labels: **Fixed** (hard to change — housing, bills, subscriptions), **Daily Living & Personal** (the discretionary middle, first place to cut), **Irregular** (lumpy, easy to forget — big purchases, travel, taxes), **Income**. No category = Uncategorized — a loose end to tidy, not an error.\n\n**Budget targets** — every category has a monthly target without the user setting one. The target is the heavier of (a) last month's spend and (b) the average over a reference window. The window (basis) is user-selectable: trailing 3 / 6 / 12 months, or the same month a year ago for seasonal categories. A category with no recent spend has no target and stays quiet. The user can set an explicit budget on any category to override the derived one; the rest keep deriving. Implicit targets are computed live, never stored. The budget is forward-looking analysis built from past data — it needs a couple of months of history (importable) to be meaningful.\n\n## What the app gives the user\n\nEach analytics view answers one question:\n- **Spending** — where money went, by category.\n- **Cash Flow** — income vs. expenses over time; net positive or not.\n- **Net Worth** — total value across accounts over time.\n- **Merchants** — which merchants take the most.\n- **Compare** — category trends against each other.\n- **Monthly Budget** — this month's spend vs. target, per category.\n\nValue scales with data: more complete, cleaner history → sharper analysis. Steering the user toward a fuller, accurate log is almost always the right long-term help.\n\n## Who the user is\n\nPeople use the app to answer the questions money raises: where does it go, can I afford this, am I getting ahead, why is the card bill so high. They want an honest picture without it becoming a second job. Most aren't accountants — they want clarity and light guidance, not methodology lectures. Some arrive from YNAB/Mint with opinions; some have never categorized a transaction in their life.\n\n## Your job\n\nYou can do anything the user can do in the app — read, create, update, and delete across accounts, transactions, categories, and budgets — plus import and analysis. In practice:\n- **Import & categorize** — turn CSVs, statement screenshots, and receipts into clean, categorized transactions; learn from how the user already categorizes.\n- **Keep the log accurate** — dedupe, fix miscategorized rows, normalize messy merchant names, catch the credit-card double-count. Accuracy is what makes every insight trustworthy.\n- **Surface patterns** — recurring/periodic charges (subscriptions, bills), spending shifts, anomalies.\n- **Answer questions** — spending, trends, comparisons, affordability — in plain language, acting directly rather than telling the user where to click.\n\n## Teaching stance (for inexperienced users)\n\n- **No imposed methodology.** Track what happened; help them decide. Meet them where they are.\n- **Correct only the few real mistakes** — the credit-card double-count, treating transfers as spending, leaving everything uncategorized. Most else is preference.\n- **Reduce upkeep, don't add chores.** Upkeep is what kills budgets.\n- **Let the data teach.** A budget built from their own history shows an honest picture faster than any explanation.\n",
  "APP_MAP.md": "# Capy — app map\n\n## How the app is laid out\n\nThe window has a left navigation rail, a top header, a main content area, and the\n\"Ask Capy\" panel (your home) on the right.\n\n- Left **navigation rail** — the primary nav: Accounts · Budget · Import up top;\n  Help · Settings at the bottom.\n- Top **header**: budget switcher (left), New Transaction (⌘N), and theme/display\n  toggles + Ask Capy (⌘I) on the right.\n- Right **\"Ask Capy\" panel** — where you live (toggle with ⌘I); the header shows the\n  active provider. A message box at the bottom takes questions and file attachments\n  (CSVs, statement images). Its bottom controls: **Commands** (the user's saved\n  prompts), **Instructions** (edit the user's custom instructions to you —\n  same editor as Settings → Intelligence), and **New chat** (reset the conversation).\n\n**Accounts** — the ledger. A left sidebar lists accounts grouped by type (cash,\nchecking, savings, credit card, loans, assets) with balances and a net-worth total;\nthe main area is the transaction table (date, account, merchant, category, amount)\nwith search, a category filter, and a date-range filter. Selecting one account\nfilters the table to it.\n\n**Budget** — the analytics, as tabs across the top: Spending · Cash Flow · Net Worth\n· Compare · Merchants · Monthly Budget. Every tab has its own period selector (a\nsubset of Month / Quarter / Year / All Time / Custom with a ‹ › stepper —\ngranularities vary by tab). The first five share an income/expenses/net summary;\nMonthly Budget instead shows a spend-vs-target KPI strip (Spent this month /\nTracking toward / Over budget). What each shows:\n- **Spending** — donut of spend by category; Expenses/Income toggle.\n- **Cash Flow** — paired income-vs-expenses bars per period; hover a period for its\n  income, expenses, and net.\n- **Net Worth** — total value over time, switchable Bar / Area; a per-account\n  filter popover narrows which accounts feed the chart, without changing your\n  saved Net Worth settings.\n- **Compare** — pick categories from a left-hand checklist; their trends draw as\n  overlaid lines to compare; Expenses/Income toggle.\n- **Merchants** — horizontal bars of top merchants by amount, with a ranked table\n  below (merchant, amount, transaction count, % of total).\n- **Monthly Budget** — this month's spend vs. each category's target.\n\n**Import** — drop CSVs or statement images, add optional instructions, pick a target\naccount, then Start Import; review and edit the parsed rows (Enrich, Merge) before\nconfirming.\n\n**Settings / Help** — bottom of the left rail.\n",
  "ARCHITECTURE.md": "# Architecture\n\n## Tech Stack\n\n| Layer | Technology | Purpose |\n|---|---|---|\n| Monorepo | npm workspaces | Package management, dependency hoisting |\n| Desktop shell | Tauri v2 | Lightweight native wrapper (~15 lines Rust) |\n| Frontend | React 19 + TypeScript | UI components and application logic |\n| Bundler | Vite 7 | Dev server and production builds |\n| Routing | TanStack Router | Type-safe file-based routing |\n| Data cache | TanStack Query 5 | Async cache with fetch, invalidate, loading states |\n| App state | Zustand 5 | Lightweight stores (app state, undo/redo) |\n| UI primitives | shadcn/ui (Radix) | Accessible components, owned by the project |\n| Styling | Tailwind CSS 4 | Utility-first CSS |\n| CSV | PapaParse | Parse/unparse CSV with type coercion |\n\n### Tauri Plugins (Desktop Shell)\n\n| Need | Plugin |\n|---|---|\n| File read/write | `@tauri-apps/plugin-fs` |\n| Folder picker | `@tauri-apps/plugin-dialog` |\n| Subprocess + open URLs | `@tauri-apps/plugin-shell` (Claude CLI adapter, external link handling) |\n| App config persistence | `@tauri-apps/plugin-store` (intelligence provider + API keys) |\n\n## Principles\n\n### All Logic in TypeScript\n\nRust is only used for Tauri plugin registration. All application logic — data parsing, validation, queries, UI — lives in TypeScript.\n\n### Functional Style\n\n- Pure functions for data transformations\n- Immutable data structures\n- Side effects isolated to adapter boundaries\n- Composable utilities over class hierarchies\n\n### Single Responsibility\n\n- A **service** (core) contains pure data transformations — no I/O\n- A **repository** (persistence) handles storage — doesn't know about UI\n- A **hook** (app) bridges data to React — no business logic\n- A **component** (app) displays data — no file I/O\n- The **intelligence layer** produces structured data — the app validates and writes\n\n### Monorepo\n\nSee `MONOREPO.md` for package layout, dependency graph, and adapter pattern.\n\n## Data Flow\n\n```\nUser picks folder (or demo loads preset data)\n  → BudgetService adapter detects/bootstraps budget\n  → CsvRepository reads CSVs via FileAdapter\n  → PapaParse with typed coercion → domain objects\n  → TanStack Query cache\n  → UI reads via query hooks\n  → mutations apply pure service functions from @capybudget/core\n  → optimistic cache update → repo.save*()\n```\n\n## Mutation Strategy\n\n### Optimistic Updates\n\n1. Validate locally. If invalid, show inline error immediately.\n2. Apply pure service function from `@capybudget/core`.\n3. Update TanStack Query cache immediately.\n4. Persist via repository in the background (debounced).\n5. On write failure: show blocking error. No retry, no partial rollback.\n\n### Undo / Redo\n\nSession-scoped stack of state snapshots (past, present, future). Mutations push present to past. Undo pops past and triggers repository write to re-sync. Not persisted across sessions.\n\n### Write Safety\n\n- Atomic writes (temp file → rename)\n- Debounced flush (rapid mutations batched)\n\n## Routing\n\nTanStack Router with file-based routing. Routes live in `packages/app/src/routes/`.\n\n- `/` — Budget selector\n- `/budget?path=...&name=...` — Budget workspace\n\n## State Management\n\n| Concern | Solution | Persistence |\n|---|---|---|\n| Budget data | TanStack Query | Repository adapter |\n| Recent budgets | Zustand | localStorage |\n| Undo/redo | Zustand | None (session only) |\n| UI state | BudgetUIContext | None (ephemeral) |\n\n## Intelligence\n\nSee `INTELLIGENCE.md` for the full intelligence layer architecture.\n\n## Testing\n\nSee `TESTING.md` for testing strategy and infrastructure.\n\n## Conventions\n\n### File Naming\n\nkebab-case for all files (e.g. `budget-shell.tsx`, `csv-repository.ts`).\n\n### Imports\n\n- `@capybudget/*` for shared packages\n- `@/` alias for app-internal imports within `packages/app`\n\n### Components\n\n- Functional components only\n- shadcn primitives in `components/ui/`, customized freely\n\nSee `STRUCTURE.md` for component decomposition, file placement, and package\ninternal layout.\n\n## Target Platforms\n\n- **Desktop** — macOS first (native .dmg via Tauri). Windows and Linux via Tauri cross-compilation.\n- **Web demo** — browser-based demo with preset data at [demo.capybudget.app](https://demo.capybudget.app). Full UI, no persistence, stub intelligence layer.\n- **Promo website** — static marketing site at [capybudget.app](https://capybudget.app). Landing page, privacy policy, documentation. Astro 6, deployed to Vercel.\n\n## Distribution\n\nOpen source, MIT license. Public GitHub repo with architecture docs, web demo, promo website, and a standalone MCP server (`@capybudget/mcp`) that works with any MCP-compatible AI agent.\n",
  "DATA_MODEL.md": "# Data Model\n\nAll data lives in a user-chosen folder as plain CSV files. A `budget.json` metadata file identifies the folder as a Capy Budget.\n\n## Folder Structure\n\n```\n~/MyBudget/\n  budget.json            ← metadata: schema version, name, currency settings\n  accounts.csv\n  categories.csv\n  transactions.csv\n```\n\n## budget.json\n\n```json\n{\n  \"schemaVersion\": 4,\n  \"name\": \"My Budget\",\n  \"defaultCurrency\": \"USD\",\n  \"currencies\": {\n    \"USD\": { \"decimals\": 2, \"symbolPosition\": \"before\" }\n  },\n  \"createdAt\": \"2026-03-07T12:00:00.000Z\",\n  \"lastModified\": \"2026-03-07T12:00:00.000Z\"\n}\n```\n\nThe schema version enables future migrations. On load, the app checks the version and runs any necessary transformations before proceeding.\n\n### Currency settings\n\nCurrency lives in two fields: `defaultCurrency`, the ISO code everything rolls up into and the value an account or transaction takes when it carries none of its own; and `currencies`, a map keyed by ISO code holding each currency's settings. **The default currency is just another entry of the same shape** — no split between \"the default's settings here, foreign settings there.\"\n\nEach entry carries display settings: `decimals` (0–2) and `symbolPosition` (`before` · `after` · `off`). A non-default entry additionally carries its `rate` against the default and a `rateSource` tag (`manual` · `seed`) recording where the rate came from; the default entry carries neither — it is the base, an implicit rate of 1.0. `rate` is `rate(currency → default)`: the value of one unit of the currency in default units. Money is integer ×100 for every currency, so the same ratio values cents: `defaultCents = round(nativeCents × rate)`.\n\nA currency earns a settings row once an account uses it, but its entry is kept even after the last account on it is deleted — re-adding the currency restores its rate and display settings intact (persist-when-empty). The entry is seeded lazily from the currency's display defaults the first time it is used; the rows shown are gated on in-use currencies, while the map retains unused entries.\n\n### Exchange rates\n\nA currency's rate against the default is resolved through a fallback chain, yielding the rate **and** a provenance tag the UI shows (whose number it is):\n\n1. **`manual`** — the user's override (`rate` with `rateSource: \"manual\"`).\n2. **`seed`** — derived from a bundled, USD-anchored seed table by division: `rate(X → D) = usdRates[D] / usdRates[X]`, where `usdRates[c]` is units of `c` per 1 USD. This handles any base, including a non-USD default. `rate(X → X) = 1`.\n3. **`unset`** — 1.0 with a quiet \"rate unset\" state, when the currency is absent from the seed table.\n\nThe seed table ships as `{ base: \"USD\", rates: { CODE: numberPerUSD, … } }` — deliberately the same shape the checkpoint-3 rate Lambda will serve, so it later becomes the offline floor under the fetched rates with no reshape. Its values are overridable fallbacks (reasonable mid-market figures), not live data. A `todayRates` map of every currency in the budget keyed to its resolved rate against the default feeds the analytics converter; flows value at the rate stamped on each transaction, balances at today's resolved rate.\n\nDisplay settings are seeded from the currency's curated defaults — `{ 0, after }` for RUB, `{ 2, before }` for USD — so a user whose exact currency isn't listed can pick a near one and match their real formatting. The symbol is currency-driven; all amounts are integers in the minor unit regardless. Decimals only rounds the rendered figure; money on disk stays ×100, so 2 is the ceiling — a third decimal could only ever render zero, and a stored value above 2 is clamped to 2 on load. The default entry's knobs are re-seeded on a currency switch — changing the default lands on the new currency's conventional formatting rather than carrying the prior tweaks; a \"reset to defaults\" control restores the current currency's defaults on demand (e.g. after manual tweaks).\n\nThe currency settings carry no schema bump: they are normalized at load time, not by a numbered migration. A budget written before they existed — whether missing them entirely or carrying the older flat `currency` / `currencyDecimals` / `currencySymbolPosition` fields — is read into the unified shape, lifting any flat fields into the default entry and backfilling missing knobs from the currency's defaults. The normalized shape is written back on the next save, which rewrites every existing budget.json (single-currency ones included); only the shape moves, so the rendered numbers and formatting are unchanged.\n\n## Accounts\n\nEvery financial entity is an account.\n\n| Field                  | Type    | Notes                                                             |\n|------------------------|---------|-------------------------------------------------------------------|\n| `id`                   | string  | UUID, client-generated                                            |\n| `name`                 | string  | User-defined label (e.g. \"BofA Checking\", \"Cash Wallet\")          |\n| `type`                 | enum    | `cash · checking · savings · credit_card · loan · asset · crypto` |\n| `archived`             | boolean | Hidden from the sidebar when true. Archiving requires a zero derived balance (see Referential Integrity), so it has no effect on current net worth, but the account's transactions still count toward the historical net-worth series at the points where it carried a balance. |\n| `excludeFromNetWorth`  | boolean | Excluded from Net Worth calculations when true                    |\n| `sortOrder`            | integer | Display ordering                                                  |\n| `createdAt`            | string  | ISO 8601                                                          |\n| `currency`             | string  | ISO 4217 code the account holds natively. Set when the account is created, defaulting to the budget default; editable only while the account has no transactions, then locked. Balances roll up into the default at conversion time; a default-currency account converts as the identity. |\n\n**No stored balance.** Balance is always derived: sum of all transactions where `accountId` matches. See Architecture for rationale.\n\n**Opening balance.** When creating an account with an existing balance, generate an \"Opening Balance\" income transaction dated to the account's `createdAt`. This is the only way balances enter the system — through transactions.\n\n## Categories\n\nFully user-manageable. Sensible defaults prepopulated on first launch.\n\n| Field       | Type             | Notes                                                             |\n|-------------|------------------|-------------------------------------------------------------------|\n| `id`        | string           | UUID, client-generated                                            |\n| `name`      | string           | Display name                                                      |\n| `group`     | string           | Logical grouping (see below)                                      |\n| `archived`  | boolean          | Hidden under \"Archived\" group                                     |\n| `sortOrder` | integer          | Display ordering within group                                     |\n| `assigned`  | integer \\| null  | Explicit monthly budget in cents. `null` = no explicit budget. `0` = tracked at zero. Single piece of mutable current state — applies to every month. The only stored budget input. |\n\n**Budget target.** The figure a category's spend is tracked against is `assigned ?? implicitTarget`. `assigned` is the explicit budget above; `implicitTarget` is derived from the category's spending history — the heavier of last month and a reference average over the months the budget-wide `basis` selects (the divisor is the count of those months with non-zero spend, so a dormant month doesn't dilute the figure), `null` when none of the reference months had spend. Last month is always the single month before the viewed one, independent of `basis`. Implicit targets are **computed at render and never stored** — there is no derived-target column and no migration. `assigned` is the only budget field that touches CSV.\n\n### Default Category Groups\n\n| Group          | Categories                                                                          |\n|----------------|-------------------------------------------------------------------------------------|\n| Income         | Paycheck, Other Income                                                              |\n| Fixed          | Housing, Bills & Utilities, Subscriptions                                           |\n| Daily Living   | Groceries, Dining Out, Transportation                                               |\n| Personal       | Alcohol & Smoking, Health & Beauty, Clothing, Fun & Hobbies, Allowances, Education & Business, Gifts & Giving |\n| Irregular      | Housekeeping & Maintenance, Big Purchases, Travel, Taxes & Fees                     |\n\nGroups can be created, renamed, and reordered by the user.\n\n## Transactions\n\nThe core entity. Every financial event is a transaction.\n\n| Field            | Type    | Notes                                                           |\n|------------------|---------|-----------------------------------------------------------------|\n| `id`             | string  | UUID, client-generated                                          |\n| `datetime`       | string  | ISO 8601. Time preserves entry order within a day.              |\n| `type`           | enum    | `income · expense · transfer`                                   |\n| `amount`         | integer | **Signed** cents. Negative = outflow, positive = inflow.        |\n| `categoryId`     | string  | UUID FK to categories. **Empty for transfers.**                 |\n| `accountId`      | string  | UUID FK to accounts. The account this transaction belongs to.   |\n| `transferPairId` | string  | UUID of the paired transaction. Empty for non-transfers.        |\n| `merchant`       | string  | Optional. Who you paid or received from.                        |\n| `note`           | string  | Optional. Additional context.                                   |\n| `createdAt`      | string  | ISO 8601                                                        |\n| `fxRate`         | number  | Optional. The account's native→default rate stamped the day the transaction happened, so flows never re-rate as rates move. Empty = a default-currency transaction = an implicit rate of 1.0. Amounts are always stored in the account's native currency; this rate values them in the default at read time. A transfer's two legs each carry their own `fxRate` (see Transfer Architecture § Cross-currency transfers). |\n\n### Sign Convention\n\n- **Expense**: amount is negative. Reduces account balance.\n- **Income**: amount is positive. Increases account balance.\n- **Transfer outflow leg**: amount is negative on source account.\n- **Transfer inflow leg**: amount is positive on destination account.\n\nZero-amount transactions are allowed — useful for tracking non-monetary events or placeholder entries.\n\nAn account's balance = `sum(amount)` for all its transactions. No special cases.\n\n### Transfer Architecture\n\nA transfer is **two linked transactions** with mutual `transferPairId` references.\n\n- Creating a transfer creates both legs atomically. User specifies source account, destination account, and amount. The system creates:\n  1. Outflow transaction (negative amount) on the source account\n  2. Inflow transaction (positive amount) on the destination account\n  3. Each leg's `transferPairId` points to the other's `id`\n- **Transfers have no category** (`categoryId = \"\"`). Enforced at schema level. Transfers move money — they are not spending.\n- Deleting either leg **cascades** to delete both.\n- Updating a transfer **propagates** amount and date changes to the paired transaction.\n- **Type changes between income ↔ expense are allowed.** Changing to/from transfer is NOT — delete and recreate. This avoids orphaned pair references.\n\n#### Cross-currency transfers\n\nWhen the two accounts hold different currencies, the legs cannot be equal and opposite — $100 leaving a USD account does not arrive as $100 in a EUR account. So a cross-currency transfer carries **two independent native amounts**, one per leg in that account's currency, and **each leg stamps its own `fxRate`** (its native→default rate), exactly as a standalone flow does. The form shows a second \"received\" amount field, prefilled from the display cross-rate and fully editable (the user enters what actually landed). A same-currency transfer is unchanged: one amount, mirrored, both legs sharing the one stamped rate.\n\nPer-leg rate rules:\n\n- A leg in the **default currency** leaves `fxRate` empty (an implicit 1.0).\n- When **exactly one** leg is the default, the foreign leg's rate is **derived from the two amounts** — the real rate the transfer executed at, more accurate than the table. From default→foreign, `toRate = fromAmount / toAmount`; from foreign→default, `fromRate = toAmount / fromAmount`.\n- When **neither** leg is the default, the amounts pin the X↔Y rate but not either →default rate, so each leg stamps its own resolver rate.\n\nThe two legs net to ~0 in the default currency at the stamped rates (any residue is the genuine FX spread), so a cross-currency transfer fabricates no net-worth gain or loss. Editing a transfer re-stamps both legs from the edited amounts and currencies.\n\n## Referential Integrity\n\n| Operation         | Rule                                                              |\n|-------------------|-------------------------------------------------------------------|\n| Delete account    | **Blocked** if account has more than one transaction              |\n| Archive account   | **Blocked** if derived balance is non-zero                        |\n| Update transfer   | **Propagates** `amount` and `date` to paired transaction          |\n| Delete transfer   | **Cascades** — deletes paired transaction                         |\n| Delete category   | **Clears** `categoryId` on all referencing transactions           |\n| Archive category  | No cascade — transactions keep the reference, display still works |\n\n## Schema Migrations\n\nThe `schemaVersion` field in `budget.json` drives migrations. On load, the app checks the version and runs sequential migration functions to bring the data up to the current schema. Migrations transform CSV structure in-place, then `budget.json` is rewritten with the new version.\n\nEach migration `n → n+1` is a pure-ish function on the budget folder, idempotent (re-running on already-migrated data is a no-op). Migrations live in `src/services/budget-migrations.ts`.\n\n### History\n\n| From → To | Change |\n|-----------|--------|\n| 1 → 2     | Add `excludeFromNetWorth` column to accounts.csv (default `false`). |\n| 2 → 3     | Add `assigned` column to categories.csv (empty cell = `null` = untracked). |\n| 3 → 4     | Add `currency` column to accounts.csv (stamped with the budget default for every existing account) and `fxRate` column to transactions.csv (empty everywhere). Behavior-identical: every account lands in the default currency, every transaction has an implicit 1.0 rate, so a single-currency budget reads back unchanged. |\n\n## Import Staging\n\nSmart Import works in a scratch area under the budget folder, `.capy/import/`. None of it is budget data — it's intermediate state that exists only between dropping files and merging, and is cleared on merge or cancel. The pipeline that produces it is specified in `IMPORT.md`; this is its data shape.\n\n```\n.capy/import/\n  sources/              # original files (CSV, image, PDF)\n  transactions.csv      # the staged rows\n  context.json          # per-row history signals\n  transfer-context.json # per-transfer-row direction-aware history\n  state.json            # phase + run metadata\n```\n\n### Staged record\n\nBoth normalization paths (a `CsvMapping` applied to a CSV, or a model reading an image/PDF) converge on one intermediate record before it's built into a staged row:\n\n| Field           | Type    | Notes                                                        |\n|-----------------|---------|--------------------------------------------------------------|\n| `date`          | string  | `YYYY-MM-DD`                                                 |\n| `amount`        | integer | **Signed** cents (negative = outflow) — sign already resolved |\n| `type`          | enum    | `expense · income · transfer`                                |\n| `description`   | string  | Raw merchant text, untrimmed at this stage                   |\n| `sourceAccount` | string  | Raw account string from the source                           |\n| `sourceCategory`| string  | Raw category string from the source (empty when none)        |\n\n`buildStaged` is the single sink: it assigns ids and trims `description` to produce the staged row.\n\n### Staged row (`transactions.csv`)\n\n| Field               | Type    | Notes                                                                                  |\n|---------------------|---------|----------------------------------------------------------------------------------------|\n| `id`                | string  | Sequential `imp-N`, continuing across multi-file imports                                |\n| `date`              | string  | `YYYY-MM-DD`                                                                            |\n| `description`       | string  | **Raw text trimmed to 45 chars** — the canonical form for both history matching and `note` at merge. Truncation drops trailing reference-number noise; there is no separate full-raw field. |\n| `amount`            | integer | Signed cents (negative = expense, positive = income)                                   |\n| `type`              | enum    | `expense · income · transfer`                                                           |\n| `sourceAccount`     | string  | Raw account string — resolved to `accountId` during grounding                          |\n| `sourceCategory`    | string  | Raw category string from the source — a weak hint to the classifier, never resolved to a category in code |\n| `merchant`          | string  | Cleaned merchant name. Empty until grounding or the classifier fills it                 |\n| `accountId`         | string  | Resolved budget account UUID (may be empty)                                             |\n| `targetAccountId`   | string  | For transfers: the counterpart account UUID. Empty until the model picks it in Categorizing (or the user sets it); empty = unmatched |\n| `categoryId`        | string  | Resolved budget category UUID. Empty until grounding or the classifier fills it         |\n| `categoryConfidence`| string  | `high` · `low` · `\"\"` — set alongside `categoryId`                                       |\n| `duplicate`         | boolean | True when the row matches an existing budget transaction — skipped by enrichment, unselected at merge |\n| `duplicateConfidence`| string | `high` · `low` · `\"\"` — the dup match tier; `low` (relaxed date window) renders as a possible duplicate to review |\n\nThere is no `memo` field. At merge, `note` is the trimmed `description` and nothing else; `merchant` on `Transaction` is reserved for the cleaned name.\n\n### History context (`context.json`)\n\nEphemeral classifier input written during grounding, keyed by row id. It feeds the Categorizing call and is never a `transactions.csv` column or persisted to `Transaction`. Rows with no historical match carry no entry.\n\n```ts\nRecord<string /* row id */, {\n  examples: {            // top-3 most-recent matching transactions\n    date: string\n    merchant: string\n    note: string\n    categoryId: string\n    amount: number\n  }[]\n  merchantStats: { name: string; count: number }[]   // merchant frequency among matches\n  categoryStats: { name: string; count: number }[]   // categoryId frequency among matches\n}>\n```\n\n### Transfer context (`transfer-context.json`)\n\nEphemeral input for the transfer-counterpart call, written during grounding, keyed by row id — transfer rows only, and only those whose imported account has matching-direction transfer history. The counterpart is a property of the *accounts*, so this carries the account's own legs in the row's direction (inflow → incoming legs, outflow → outgoing), each by counterpart account **name**. Like `context.json`, it never becomes a `transactions.csv` column or touches `Transaction`.\n\n```ts\nRecord<string /* row id */, {\n  recentTransfers: { account: string; amount: number }[]  // last 2 months, chronological, ≤10\n  topAccounts: { account: string; count: number }[]       // top-3 counterparts by count in this direction\n}>\n```\n\n### Run state (`state.json`)\n\n```ts\n{\n  phase: ImportPhase           // reading · normalizing · history · categorizing · done · error · idle\n  rowCount?: number            // set once staging is written\n  updatedAt: string            // ISO timestamp of the last write\n  source?: \"chat\"              // present when the chat on-ramp staged the run; its absence marks a manual Import-tab drop\n}\n```\n\n### Aliases (`.capy/aliases.json`)\n\nSurvives across imports, so returning users don't re-map the same accounts. Persisted at merge.\n\n```ts\n{ accounts: Record<string /* sourceAccount */, string /* accountId | \"__create__\" */> }\n```\n",
  "FRONTEND_DESIGN.md": "# Frontend Design\n\nVisual design, interaction patterns, and accessibility guidelines.\n\n## Color Scheme\n\nThe app uses **OKLCh color space** for perceptually uniform colors. Every theme defines a full set of CSS custom properties (brand, neutrals, semantic, sidebar, charts) in both light and dark variants.\n\n### Architecture\n\n- **Light/dark mode** — managed by `next-themes`, toggled via a three-state cycle (light → dark → system). Applies `.dark` class to `<html>`.\n- **Color themes** — managed by `ColorThemeProvider`, persisted to `localStorage` under `color-theme`. Applies `data-theme` attribute to `<html>`.\n- Both are independent — any color theme works in light or dark mode.\n\n### Themes\n\n| Theme    | Character                          | Hue  |\n|----------|------------------------------------|------|\n| Capybara | Warm amber/sand — the default      | ~55  |\n| Ocean    | Cool blue                          | ~240 |\n| Forest   | Green/emerald                      | ~152 |\n| Rose     | Pink/mauve                         | ~350 |\n| Slate    | Minimal chroma, neutral cool gray  | ~260 |\n\n### Design Principles\n\n- **Hue-tinted neutrals.** Every gray in a theme carries a subtle tint of the theme's hue, not pure gray. This gives warmth (Capybara) or coolness (Ocean) to the entire UI.\n- **Semantic colors are stable.** Expense (terracotta), income (sage), and destructive (red) stay the same across all themes — they carry meaning, not branding.\n- **Perceptual uniformity.** OKLCh ensures that colors with the same lightness value actually look equally bright, so light/dark variants are consistent across themes.\n\n### Adding a New Theme\n\n1. Add an entry to `packages/app/src/lib/color-themes.ts` with a label and swatch color.\n2. Add `[data-theme=\"<name>\"]` and `[data-theme=\"<name>\"].dark` blocks in `packages/app/src/styles/index.css`, following the existing pattern (shift hue, keep luminance/chroma structure).\n\n## Navigation\n\nThree top-level sections: **Accounts**, **Budget** (categories), and **Import**.\n\n- **Desktop**: vertical navigation rail (left edge, 64px) with icon+label links. The header spans full width above the rail.\n- **Mobile**: bottom tab bar with the same three sections.\n- **Sidebar**: scoped to the Accounts section only. Slides in/out via its own edge handle. Budget and Import sections get full content width.\n- **Settings**: lives in a bottom utility cluster on the desktop rail (gear icon), kept separate from the primary three to signal \"infrequent / configuration.\" Intentionally not surfaced in the mobile bottom-tab bar — mobile is a read-mostly surface and the rail's vertical room doesn't translate there.\n\nCapy is shipped a static app map (`specs/APP_MAP.md`) so it can answer \"where do I find X\" without guessing. It mirrors this structure — sections, the analytics tabs, the nav layout, and each tab's chart and controls. Re-check it whenever the UI changes structurally so it doesn't silently drift from the real app.\n\n## Chat Panel\n\nCapy's chat panel slides out from the right edge — full-width on mobile, default 440px on desktop with a draggable left edge (max `min(50vw, 720px)`, width persisted). Non-modal: the rest of the app stays interactive while the panel is open.\n\nThe header (mascot avatar, name, status, \"New chat\", close) is persistent across every state. The close icon is \"Hide panel\" — collapsing the panel preserves the session; \"New chat\" is the explicit reset.\n\nEmpty states:\n- **Unconfigured** (`provider === null`): mascot + setup copy + provider quick-pick chips + \"Open settings\". Input is hidden until a provider is configured.\n- **Welcome** (configured, no messages): mascot + greeting + four suggestion cards (click sends prompt as user message).\n\nConversation:\n- Tool calls render as a stacked card with spinner→checkmark; the card persists in history.\n- Money and percentages get inline emphasis via lightweight `**bold**` parsing rendered in brand color (bold/italic XOR — not nestable).\n- Follow-up suggestion chips appear after responses (model-driven via `render_followups`) and click-to-send.\n\n## UX Principles\n\nKeyboard-first workflow for the majority of user actions. Controls are intuitive — ESC closes modals, Enter confirms, Tab navigation in forms makes sense.\n\n### Transaction Entry\n\n- **Hero amount input**: large text, type-aware coloring (red/green/neutral).\n- **Segmented type control** (expense/income/transfer) with semantic colors.\n- **Keyboard shortcuts in amount field**: `-` → expense, `+` → income.\n- **Merchant autocomplete**: typeahead from past merchants (word-start priority), selecting a known merchant auto-fills the category.\n- **Convenient date input.**\n- **Pre-select the currently viewed account.**\n\n### Transaction List\n\n- Columns: date, account, merchant, category, amount.\n- Sortable by any column. Inline editing — click to edit.\n- Full-text search across all visible fields, including money e.g. \"12.50\" finds transactions with amount=1250. Category filter, date range picker.\n- Secondary filters (transaction type, uncategorized-only, no-merchant-only) live behind a filter-icon popover; a dot on the trigger marks an active subset.\n- \"All Accounts\" view for cross-account overview.\n- Transfers don't have category/merchant columns and display from/to accounts instead.\n\n### Transactions Browser (Drilldown)\n\nAnalytics surfaces — Monthly Budget, Spending, Merchants, Compare — each render aggregate numbers. Every meaningful aggregate is a drilldown into the underlying transactions: clicking opens a **read-only, pre-filtered transactions popup** (modal, portalled, ESC + click-outside close) showing exactly the rows that produced that number.\n\n- **Read-only.** No inline editing, deletion, selection, or bulk actions in the popup. Editing remains the job of the account-detail view.\n- **Pre-filtered, locked context.** The caller filters the transactions array and passes locked-filter chips for display (category, merchant, date range). Chips are not removable — to broaden, the user closes the modal.\n- **Conditional search.** A local substring search field (across merchant, category, note, and amount in both formatted and raw forms) appears only when the list has **more than 10 rows**. Below that threshold a search box is clutter and the eye can scan faster.\n- **Virtualization** carries over from the shared `TransactionList` — the popup handles a category with hundreds of transactions without jank.\n\nDrilldown points wired this pass:\n\n| Surface | Click target | Locked filter |\n|---|---|---|\n| Monthly Budget | \"Spent\" cell on a category row (when > $0) | category + month |\n| Spending | Pie slice (current view mode: expenses or income) | category + period |\n| Merchants | Merchant name in the ranked list, or its bar in the chart | merchant + period |\n| Compare | A month/week point on the trend chart | selected categories + bucket window |\n\nThe same component is shaped to render later as a Capy-chat block (no modal chrome) and behind a `/transactions` deeplink route. Modal-only this pass.\n\n### Monthly Budget\n\nEvery expense-side category (Income excluded) shows a live progress bar tracking this month's spend against a target — without anyone assigning a number first. The target is `assigned ?? implicitTarget`: an explicit budget when the user sets one, otherwise a target derived from the category's own spending history. See `PRODUCT.md` for the model and `DATA_MODEL.md` for where the number comes from.\n\n**KPI strip** (three cards): **Spent this month** (all categorized expense, drills into the transactions), **Tracking toward** (sum of effective targets across all rows), **Over budget** (count of rows whose spend exceeds their target).\n\n**The zoned bar.** Each row's bar is a small infographic, not a percent meter:\n\n- **Two zones.** A green \"within\" zone (0 → target) and a red \"over\" zone (target → off the end), split by a target divider sitting at a fixed fraction of the track width — the same fraction on every row. Because the divider is positionally fixed, the over/under line scans straight down the column: a row is over budget exactly when its fill crosses the divider, regardless of dollar size.\n- **The fill** grows with spend and is **binary green up to and including the target, red only past it**. Reaching 100% of a target is on-budget, not a warning — rent at exactly its target reads green. Overshoot beyond the target is log-compressed into the over-zone so an extreme overspend can't blow out the layout.\n- **History pins.** Up to two diamond markers float above the track on the bar's scale: a **filled** diamond for last month's spend, a **hollow** one for the comparison basis (the reference average). Hover or keyboard focus reveals the exact figure.\n- **The divider** is **dashed and ghosted when the target is auto-derived**, **solid and strong when the user set it** — so you can tell at a glance which targets you own and which Capy inferred.\n- **Untargeted rows** (no budget, no usable history) render a calm faint dashed track with no zones, fill, pins, or divider — never red. They sit at reduced opacity and carry a muted dot rather than the brand dot. The bar only signals state; the spend itself lives in the Spent column.\n- **Explicit zero target** (\"don't spend here\") collapses the green zone to nothing: the divider sits at the left edge and any spend reads as a full red bar.\n\n**Remaining column.** A tracked row shows target-minus-spend; an overspend reads as a signed negative (\"-$88.30\") in the expense token, so the minus sign carries the \"over\" meaning without relying on color. An untargeted row shows an em-dash.\n\n**Legend & comparison basis.** A compact, right-aligned key for the two history pins, pairing each glyph with text so the markers never rest on color alone. It sits near where the pins render and appears once at least one row draws a real bar. The filled last-month item is static. The hollow-diamond item doubles as the **comparison-basis picker**: a dropdown whose label is the resolved basis — `3-mo avg`, `6-mo avg`, `12-mo avg`, or the named month for \"same month last year\" (e.g. `Dec 2024`) — with a chevron. Its menu offers the four bases (*3 months · 6 months · 12 months · same month last year*); the trailing options average the active months in that window, \"same month last year\" reads the single month a year before. The choice is a local display preference, remembered per device like the color theme — it lives in the browser, not the budget file, so it isn't synced across machines. Picking a basis recomputes every row's reference pin, its auto-target, and the \"Tracking toward\" KPI in place; the filled last-month pin and the `max(last month, reference)` target rule are unaffected. The zones, fill, and divider are visually self-evident and carry no legend row.\n\n**First month & filtering.** On a first month with no spending history, every implicit target is null; an inline note frames the empty bars as \"forming\" rather than broken and points at this month's spend. A \"Show only tracked\" toggle collapses the view to the rows Capy is actively tracking (explicit or implicit target), a counter reads `N of M tracked`, and each group header shows an `N/M tracked` count. The toggle and counter appear only once some rows are tracked and others aren't.\n\n**Editing a target.** The Target cell is click-to-edit, with the amount pegged to the column's right edge so every row's figure aligns. An explicit budget shows its amount; an implicit one shows an \"auto\" tag to the left of the auto-derived number (so the user sees Capy's inferred figure and can override it); an untargeted row shows a quiet \"set\" affordance. Clearing the input reverts to auto/untargeted; `0` commits as an explicit zero target.\n\n### Confirmation Dialogs\n\nBe explicit about consequences. Warn when deleting a transfer (both legs go), deleting a category (N transactions affected), or explain why an archive is blocked.\n\n### Empty States\n\nBlank states guide the user toward the next step: prompt to create a first account, a first transaction, or explain empty filter results. When adding a transaction with no accounts, intercept and trigger account creation.\n\n## Typography\n\n- Font: Geist Variable (sans-serif).\n- Tabular figures (`font-variant-numeric: tabular-nums`) for all financial amounts. Numbers must align vertically in columns.\n\n## Accessibility\n\n- 44px minimum touch targets (WCAG 2.5.5).\n- No hover-only interactions — everything works on keyboard.\n- Active navigation: `aria-current=\"page\"`.\n- Icon-only buttons: `aria-label`.\n- Semantic color tokens for amounts, not raw color values.\n",
  "I18N.md": "# Internationalization\n\nCapy Budget ships a localized UI. English (`en`) is the source language and\nfallback; Russian (`ru`) and Spanish (`es`) are the shipped translations.\nAdding a language is a translation-only contribution — see\n`packages/i18n/CONTRIBUTING_TRANSLATIONS.md`.\n\n## Package\n\n`@capybudget/i18n` owns all translation infrastructure and is consumed by both\n`packages/app` and `apps/demo` — one source of truth, no duplicated catalogs.\nIt exports the i18next config (self-initializing on import), the `I18nProvider`,\nthe `useTranslation` / `useLocale` / `useFormatLocale` / `useAiLanguage` hooks,\n`setLocale`, and the supported-locale metadata.\n\n## Catalogs\n\nJSON, namespaced by area: `packages/i18n/locales/<lang>/<namespace>.json`.\nNamespaces: `common` (nav + shared action buttons), `settings`, `budget`,\n`analytics`, `import`, `capy`, `onboarding`, `help`, `demo` (the web demo's\nscenario picker and notices). Keys are grouped by feature within a namespace,\nnot flattened.\n\nThe Help screen is a hybrid: its short chrome (header, section anchors, demo\nlink) lives in the `help` namespace, but the long-form \"How budgeting works\"\nprose lives in per-locale content modules\n(`packages/app/src/components/help/help-guide-content.<lang>.ts`, selected off\nthe active locale) — paragraphs of copy don't belong in catalog JSON.\n\n`react-i18next.d.ts` derives `t()` key types from the `en` catalogs, so keys are\nchecked under strict mode and autocomplete — a wrong key fails typecheck. Other\nlocales are validated for key parity at runtime, not by types.\n\n`resources.ts` imports the `en` catalogs statically (they're the typed source of\ntruth) and discovers every other locale's catalogs with `import.meta.glob`,\nkeyed off `SUPPORTED_LOCALES`. So adding a language is a JSON-plus-one-registry-\nline change — there is no hand-maintained per-locale import block to forget, and\na contributor can't ship a locale that silently falls back to English.\n\n## Namespace convention\n\nA component declares its **primary area namespace first, then `common`**, and\nreaches shared keys with the `common:` prefix:\n\n```ts\nconst { t } = useTranslation([\"budget\", \"common\"])\nt(\"transactions.title\")   // budget — the primary namespace, unprefixed\nt(\"common:actions.save\")  // shared button, explicitly prefixed\n```\n\nA component that only needs one namespace passes it directly —\n`useTranslation(\"settings\")` or, for the nav (which is pure `common`),\n`useTranslation(\"common\")`. Never rely on the bare `useTranslation()` /\n`defaultNS` form: name the namespace so the lookup is explicit and greppable.\n\n## Language vs format locale\n\nOne locale has two projections. **Language** is the i18next catalog language\n(`en`/`ru`) — it drives `t()`, `useAiLanguage`, and help-content selection, and\nis collapsed to the supported set with `en` fallback via `normalizeLocale`.\n**Format locale** is the full BCP-47 tag (`en-GB`, `ru-RU`) passed to every\n`Intl` formatter; it carries the region. Decoupling them lets one shared\ntranslation catalog serve regional formatting variants — a Mexican (`es-MX`) and\na Spaniard (`es-ES`) read the same Spanish UI but get their own number grouping.\n\nThe region is auto-detected, never persisted separately: on the Tauri shell\n`navigator.language` is already the OS locale, so `detectInitialLocale` keeps\nthat full tag as the format locale while collapsing its primary subtag to the\ncatalog language. The keep is gated on language support — a region tag survives\nonly when its language is supported (`en-GB` stays `en-GB`; `fr-FR` collapses to\n`en`), so formatting never drifts ahead of the displayed UI language. i18next\nholds both: `supportedLngs` + `nonExplicitSupportedLngs` resolve a region tag to\nits base catalog while `i18n.language` retains the region. `useLocale()` returns\nthe language; `useFormatLocale()` returns the format tag; both react to a\nswitch.\n\n## Locale preference\n\nLanguage is a device/user preference (not per-budget): persisted in\n`localStorage` under `capybudget.language`. On first run it defaults to the\ndetected webview/OS locale (`navigator.language`) — full tag for the format\nlocale, primary subtag for the language. The Settings ▸ General **Language**\npicker sets the **language** only (generic regional conventions for that\nlanguage) and persists it via `setLocale`; it applies live via\n`i18next.changeLanguage`, and `useLocale` / `useFormatLocale` re-render consumers\non the switch. A per-region override picker would be an additive future control;\nreal users get their region from the OS. (Currency, by contrast, stays\nper-budget in `budget.json`.)\n\n## Formatting\n\nMoney and dates are locale-aware without coupling `@capybudget/core` to app\nstate: the format locale is threaded in as a parameter. `formatMoney` /\n`formatMoneyCompact` and `formatDateLabel` / `formatMonthLabel` take an optional\nlocale (default `en-US`); render-edge consumers source it from\n`useFormatLocale()` — via the currency context (`useFormatMoney`),\n`useFormatters`, `useFormatPercent`, and the analytics label adapters. Under\n`ru`, amounts render with space grouping and a comma decimal (e.g.\n`1 250,00 ₽`); the currency symbol and its position are unchanged. Region drives\nthe rest: `en-GB` writes `10 Mar 2026` where `en-US` writes `Mar 10, 2026`.\nPlurals use i18next's `Intl.PluralRules` (`one`/`few`/`many`/`other`) off the\ncatalog language.\n\nThe calendar's first day of the week is derived from the **format locale's\nregion** via `Intl.Locale` week info — Spain/UK/Russia start Monday, US/Mexico\nstart Sunday — while the base language supplies the date-fns locale object for\nmonth/weekday names. So `es-MX` and `es-ES` read the same Spanish names but\ndiffer in week start. `resolveCalendarLocale` (`components/ui/calendar-locale.ts`)\nis the pure, tested mapping; it falls back to the name locale's own default if\n`Intl` week info is unavailable.\n\n### `useFormatters()`\n\n`hooks/use-formatters.ts` is the single render-edge entry point for\nlocale+currency-bound formatting. It returns a pre-bound bundle — `{ money,\nmoneyCompact, percent, date, month, monthShort }` — composing the existing\n`useFormatMoney` (currency context), `useFormatPercent`, `useFormatLocale`, and\nthe core date helpers as internals. The public surface is small and the\ninternals are swappable; components read `money(cents)` / `date(isoYmd)` instead\nof re-threading locale and currency at each call site.\n\nThe seam matters: `money` / `moneyCompact` read the currency context, so the\nhook is only valid under a `CurrencyProvider` (the budget surface). `percent` /\n`date` / `month` / `monthShort` depend on the format locale alone. A surface\noutside the currency context that needs only those may keep calling\n`useFormatPercent` / `useFormatLocale` directly. Range / week /\ndrilldown-subtitle labels take a per-call\ntranslator and stay in the analytics adapters (`format-range.ts`,\n`use-analytics-labels.ts`), not in this hook.\n\n## Canonical-display translation\n\nSome user-facing strings originate in `@capybudget/core`, not the catalogs:\ncategory and group names are free strings the user can rename, account-type\nlabels key off a fixed enum, and analytics bakes English fallbacks\n(\"Unknown\"/\"Uncategorized\") and month labels into its series. Core stays\nlocale-agnostic and keeps emitting canonical English — series-matching joins\n(the compare chart, import/AI matching) depend on those stable strings — so the\nlocalization happens at the render edge, by canonical match.\n\nThe app owns a central display layer:\n\n- `lib/display-names.ts` holds the canonical name set (the seeded category names\n  and the group names, including the `Archived` sentinel) and exposes\n  `useCategoryDisplayName()` / `useGroupDisplayName()` / `useAccountTypeLabel()`.\n  A display hook returns `(stored) => isCanonical(stored) ? t(key) : stored`: a\n  stored name that exactly equals a canonical default renders its translation,\n  and a renamed or user-created name renders verbatim, never translated. The\n  catalog keys live in the `budget` namespace (`canonicalCategory.*`,\n  `canonicalGroup.*`, `accountType.*`), with the canonical English string as\n  both the key and the `en` value. The enum→key lookups are typed key-maps\n  (see below), so the compiler — not a runtime test — pins each tuple to its\n  enum and each value to a real key.\n- `components/budget/analytics/use-analytics-labels.ts` localizes the\n  analytics-emitted strings the same way: `useCategorySeriesLabel()` maps the\n  empty-id Uncategorized bucket and canonical categories to `analytics:*`,\n  `useMonthLabel()` / `useWeekLabel()` re-derive a locale-aware chart-axis label\n  from the point's ISO `date` (the English `month` field core carries is never\n  shown), and `useBasisLabel()` / `useBasisOptionLabel()` localize the budget\n  basis options. The \"Unknown\" merchant bucket localizes off `getTopMerchants`'s\n  `isUnknown` flag rather than a display-string compare.\n\nThe principle is uniform: **core data stays canonical English; only the\nrendered string is translated, and only when it matches a canonical default.**\nThe stored CSV value is unchanged everywhere else.\n\n## Typed key-maps\n\nMapping an **enum or core-derived name set** to a catalog key is done with a\n**typed key-map**, not a bare dynamic `t(\\`foo.${id}\\`)` — the map is what keeps\nthat mapping checked. A map literal typed `satisfies Record<Enum, NsKey>` —\nwhere `NsKey` is `lib/i18n-keys.ts`'s `BudgetKey` / `AnalyticsKey` /\n`SettingsKey` / `CapyKey` / `DemoKey` (each `ParseKeys<ns>`, i18next's union of\nthat namespace's valid keys) — makes the compiler enforce two things at once:\nexhaustiveness (every member is mapped) and key validity (every value resolves\nto a real key). Keys are unprefixed, matching `t()` called with that namespace\nas primary. The account-type / canonical-category / canonical-group lookups\n(`display-names.ts`), the basis labels (`use-analytics-labels.ts`), the\nsettings-section labels (`settings-screen.tsx`), the tool labels\n(`tool-labels.ts`), and the demo scenario labels (`profiles/labels.ts`) all\nfollow this pattern. Because the types now guarantee what runtime lockstep tests\nused to assert, those tests are gone; the remaining display tests cover behavior\n(canonical-match vs verbatim passthrough, translation correctness).\n\nThe typed map earns its keep where coverage matters: a value the compiler must\nprove exhaustive (a core enum, the seeded categories) or pin to core so it can't\nsilently drift. It is not a blanket ban on dynamic keys. A `t()` interpolating\nover a *local* constant array or a small closed union — the analytics tab ids,\nthe symbol-position toggle, the date-range period buttons — is fine: the id set\nsits right beside the call, a typo is obvious in review, and wrapping it in a map\nwould only add indirection without buying type-safety against drift.\n\n## Semantic-label adapters\n\nBeyond the canonical-display hooks, structural and formatted copy is reached\nthrough **semantic-label adapters** — `labels.allAccounts()`,\n`labels.transactionCount(n)`, `analyticsLabels.month(date)` — so a component\nnames the concept, not the catalog key. `lib/use-budget-labels.ts` covers the\nbudget shell's route chrome (the All-Accounts header, its transaction count,\nthe account not-found state), shared by both the `packages/app` and `apps/demo`\nroute mirrors.\n\nThe boundary is deliberate. An adapter earns its place only for (a) domain\nconcepts with data/matching semantics, (b) formatted values, and (c)\nstructural or recurring chrome that's easy to miss in a sweep — route headers,\nempty / not-found states, axis labels. **Ordinary one-off leaf copy stays raw\n`t()`.** Wrapping every string in an adapter is an anti-goal: it buries the\ncatalog key behind indirection without buying type-safety or reuse. Adapters\nextend the existing modules (`display-names.ts`, `use-analytics-labels.ts`) or\nlive in a focused `use-*-labels` module per area; they are not one mega-object.\n\n## AI language\n\nCapy *chat* replies in the active language. The English name of the language\n(`useAiLanguage`) threads into the chat system prompt (`buildSystemPrompt`),\nwhich adds a single respond-in-language instruction for any non-English\nlanguage. Chat is a conversational reply and writes through category IDs / tools,\nnot by name-matching, so localizing it is safe. Tool descriptions and enum values\nstay English — the models are cross-lingual. The active language joins the chat\nsession signature, so switching rebuilds the session.\n\nThe **structured import pipeline stays canonical / source-language**, never the\nUI language: `buildImportSystemPrompt()` takes no language and emits no\nrespond-in-language instruction. Two invariants forbid localizing it. The\nreturned category is resolved back to a `categoryId` by **exact case-insensitive\nname match** against stored category names (`enrichBatch`), so a translated name\nfinds no id and the row falls through to uncategorized. And the cleaned merchant\nname is **stored data** — translating it would corrupt the canonical CSV. So the\nenrich prompt instructs the model to copy one exact category name verbatim and to\nkeep the merchant in the source statement's own language. A Russian statement\nyields Russian merchants, a US statement English ones — source-faithful, rendered\nverbatim. Category names still localize at the display edge via the canonical-\nmatch layer (see Canonical-display translation); the stored value is untouched.\n\n## Guardrail\n\n`npm run i18n:check` validates key parity across every non-`en` locale against\n`en`, per namespace, failing on any missing or extra key. It also cross-checks\nthe `locales/` directories against `SUPPORTED_LOCALES`, so a registered locale\nwith no catalog (or a catalog for an unregistered locale) fails too. On top of\nbase-key parity it checks plural *completeness*: every plural group must cover\nexactly the categories that locale's `Intl.PluralRules` declares (`one`/`other`\nfor `en`, `one`/`many`/`other` for `es`, `one`/`few`/`many`/`other` for `ru`) —\nbase-key parity strips the suffix and can't see a missing required form, so this\nis a separate pass. The\nparity logic lives in `scripts/parity.ts`; `check-parity.ts` is the CLI wrapper\nand `parity.test.ts` runs the same assertion under vitest. So drift fails the\nnormal `npm test` gate and CI's dedicated `i18n:check` step — not just a\nseparately remembered command.\n",
  "IMPORT.md": "# Smart Import\n\nDrop a file, code normalizes and grounds it, the model categorizes the ambiguous remainder, you review and merge. The app is fully functional without import — it's additive.\n\nImport requires the Anthropic or OpenAI provider. Both run the structured model calls the pipeline needs; the Claude Code CLI structured-call path is not available, so the import surface is gated off when the CLI provider (or no provider) is selected.\n\n## Architecture: code orchestrates, the model is a stateless function\n\nThere is no import agent. **Code runs the pipeline as a deterministic state machine** and emits every status line itself. The model is called **statelessly**, each call returning structured output — no tools, no loop, no accumulated context:\n\n1. **Mapping / extraction** (Normalizing) — CSV: headers + samples in → a `CsvMapping` out (one call; one bounded re-call when a code-side preview surfaces transform errors). Image/PDF: the bytes in → the same intermediate records a mapping would produce.\n2. **Categorizing batch** — ~25 rows + their pre-attached history context in → `{ id, merchant, category, confidence }[]` out, where `category` is a budget category *name* (the model reasons over names, never ids; code maps the name back to a `categoryId`). Batches are independent, run bounded-parallel, and fail in isolation.\n3. **Transfer batch** — the transfer rows + each one's direction-aware transfer context in → `{ id, account, confidence }[]` out, where `account` is a budget account *name* the model picks for the counterpart (or `\"\"` when unsure; code maps the name back to an `accountId`). A heterogeneous sibling of the Categorizing batch — transfers carry no merchant/category, only a \"From\"/\"To\" account to resolve — run in the same phase.\n\nStateless calls never accumulate: mapping is tiny, each batch is bounded. The progress surface is therefore state, not prose — code always knows where the run is.\n\n## Two on-ramps, one pipeline\n\nUser-pressed **Start Import** (the Import tab) and Capy-initiated import (chat) are two doors into the **same** staging pipeline. Capy never inserts file data into the budget directly.\n\n- **Import tab** — a drop zone takes CSV, image, and PDF files; they're written to `.capy/import/sources/` on drop. A **Start** button becomes active once files are present. A manual drop writes only `sources/` — never `state.json` — so the screen waits for Start.\n- **Chat** — when a file arrives in a chat turn, Capy calls the `start_import` tool, which copies the turn's attachments into `.capy/import/sources/`, writes a `state.json` marked `source: \"chat\"`, and replies pointing the user at the Import tab. The Import screen, seeing sources with no `transactions.csv` and a `state.json` it didn't write, auto-runs the orchestrator. The bytes live on the chat turn (text inlined, images base64); the handler stages from the turn's attachments, never from anything the model echoes into its arguments. When staging is already occupied — a run in flight or parked for review, or files dropped in the Import tab but not started — the tool refuses with guidance pointing at the Import tab instead of replacing that import; only a prior chat staging that never ran is replaced by a re-share.\n\nBoth on-ramps converge on the same `.capy/import/` staging and the same orchestrator. The on-ramps stay distinguishable by which artifacts exist, not by a flag the UI tracks.\n\n## Pipeline\n\nFour phases, all driven by code; the model is a pure function called inside two of them.\n\n```\nReading → Normalizing → History → Categorizing\n```\n\n- **Reading** — load the source files. Near-instant.\n- **Normalizing** — the mapping / extraction model call → code transforms every row → `buildStaged`. Held in memory; staging isn't written yet.\n- **History** — deterministic grounding (no AI): fuzzy-match each row against the user's own history, attach context, fold in duplicate detection, fast-path the strong-signal rows. The first staging write happens here (`transactions.csv` + `context.json` together), so \"`transactions.csv` exists\" always means normalized **and** grounded.\n- **Categorizing** — the only model batch work, over the ambiguous remainder.\n\nAccount resolution (matcher + aliases) is a step inside grounding, not a phase of its own.\n\nThe progress bar presents the four phases as two segments — **Normalizing** (Reading + Normalizing) and **Enhancing** (History + Categorizing) — each with a live \"X of N\" meter: the first fills from streamed extraction rows and CSV row counts (`normalize-progress` events), the second from landed classifier batches (`batch-progress`). The run control sits on the bar's right edge: **Stop** while a run is in flight, **Enrich N** when idle rows still need the classifier.\n\n### Normalizing — unstructured is pre-processed, not branched\n\nThere is one downstream pipeline. Unstructured input is **pre-processed into the same intermediate record the CSV mapper produces** — Capy *is* the column-mapper for a source that has no columns. After Normalizing, no phase knows or cares how a row was sourced.\n\n- **Structured (CSV):** code first locates the table's real header — a consistency scan over the parsed rows picks the row whose field count the following rows sustain (bank exports often prepend a summary preamble), and blank or duplicate header cells are renamed positionally (`Column 2`) so every column stays addressable. The model returns a `CsvMapping` (date column + format, description column(s), how amounts are structured — single signed column or split debit/credit — and formatted, how to detect expense/income/transfer, the source account, the source category column, skip rules for non-transaction rows, and an optional `headerRow` overriding the detected header when code picked wrong). Code applies it to every row. If a code-side preview of the first rows surfaces transform errors, the model gets one correction round with the errors attached; whatever the second mapping produces is final.\n- **Unstructured (receipts, bank screenshots, statement scans):** the model reads the image (image bytes as base64 `image` content, PDF bytes as base64 `document` content) and emits the same intermediate records directly — `{ date, amount, type, description, sourceAccount, sourceCategory }`. The merchant it reads becomes `description` (the match key); a category it infers becomes `sourceCategory` (where a CSV's category column lands). A bank screenshot comes out indistinguishable from a transformed CSV. The extraction call **streams**: the model declares its transaction `count` first (the schema orders it before `rows`), then the rows, so the Normalizing meter fills against a real denominator while the long extraction runs — extraction is the phase's long pole and would otherwise sit visually frozen.\n\nThe resolved `merchant` / `categoryId` fields stay **empty** in both paths — those are History's and Categorizing's job. So there are no pre-filled candidates to reconcile: Capy's receipt reads ride the raw `description` + `sourceCategory` channels and get resolved by History exactly like CSV signal.\n\nBoth paths feed one builder — `buildStaged(records) → ImportTransaction[]` — that owns id assignment (sequential `imp-N`, continuing across files), the trim-45 `description`, sign/type normalization, and empty resolved fields. One staging format, one set of invariants.\n\n### History — deterministic grounding\n\nCode pre-computes every groundable signal so the model handles only the ambiguous remainder. For each non-transfer row:\n\n1. **Fuzzy match** the row's description against the user's transaction history. History is indexed once per run; each historical transaction contributes two candidates — its cleaned `merchant` and its prior `note`. Matching runs on a normalized **match key** derived from the description: lowercased, whitespace-collapsed, with leading processor noise (card prefixes, gateway tags like `SQ *` / `TST*`) and trailing reference-number noise (auth codes, card tails, appended dates) stripped. New rows and historical text normalize through the same key, so signal lines up trimmed-vs-trimmed. Candidate retrieval is inverted-index driven (token + trigram), then ranked by combined token (Jaccard) / trigram (Dice) similarity. A historical transaction is one match — its best-scoring field, merchant-preferred on ties.\n2. **Attach context** — the top-3 most-recent matching examples plus distilled merchant / category frequency stats, written to `context.json` keyed by row id. This is the classifier's input, never a staging column.\n3. **Resolve source account** — `sourceAccount` → budget account (name match, then aliases). The resolution is written onto the row's `accountId`.\n4. **Payment-leg recognition** — an outflow expense whose description both carries a payment keyword (`payment`, `pmt`, `pymt`, `epay`, `autopay`) **and** unambiguously names exactly one *other* budget account is a card payment — one leg of a transfer between the user's own accounts, which staged as an expense would inflate spending and double-count once the card's own statement is imported. The row is retyped to `transfer` with that account prefilled as the counterpart (no model involvement). Naming is containment-scored over the account's tokens/trigrams within the description, so a concatenated bank spelling (`APPLECARD` → \"Apple Card\") still matches; a one-short-token name like \"Cash\" never participates, and two accounts clearing threshold is ambiguity — no action. A recognized leg takes the transfer treatment below (no merchant/category), and one that also duplicates an existing transaction stays a transfer with `duplicate` set.\n5. **Fast-path** — a row whose history is strong **and** near-unanimous (≥3 matches with ≥80% agreeing on one category) gets its merchant + categoryId assigned in code at `high` confidence. Everything else is left ambiguous for the classifier.\n6. **Duplicate detection** — folded in after the steps above, so it can use the merchant each row just resolved. A row is a duplicate when it matches an existing budget transaction on amount + account + relaxed dates + resolved merchant/description. Claiming is greedy 1:1 in two passes sharing one claimed set: every row tries the exact high-confidence rules before any row may claim through the relaxed ±3-day window, so a near-date match can't steal an existing transaction another row matches exactly. Each match carries its confidence (`high` for the exact rules, `low` for the relaxed ones) onto the row as `duplicateConfidence`. Duplicates are marked complete (carrying the matched transaction's merchant, and its category when type-correct, for display) and skipped by enrichment.\n\nFor each **transfer** row, History attaches a separate sidecar instead — the imported account's own **direction-aware transfer context**, written to `transfer-context.json` keyed by row id. The counterpart is a property of the *accounts*, not the row's words, so this carries the account's matching-direction legs (an inflow row → the account's *incoming* legs, an outflow → its *outgoing*), each resolved to its counterpart account **name** via `transferPairId`:\n\n- **`recentTransfers`** — the matching-direction legs from the last 2 months (anchored to the newest leg so a periodic cadence stays visible regardless of when the import runs), chronological, condensed to `{ account, amount }`, capped at 10.\n- **`topAccounts`** — the top-3 counterpart accounts by transfer count in that direction, with counts.\n\nA counterpart that's archived, gone, or the account itself is dropped (the model can't pick an id it won't see in the dropdown). An account with no matching-direction history gets no entry — the model leaves the counterpart blank. Recognized payment legs aside, grounding sets no `targetAccountId`; the model picks it in Categorizing.\n\nHistory reports the payoff: *\"47 of 77 resolved from your history · 4 duplicates.\"*\n\n### Categorizing — a batched classifier\n\nThe only model batch work, over the rows that still fail the `needsEnrich` predicate (see **Staging & resume**). Rows are split into batches of ~25 and run with bounded parallelism (4 at a time). **The model reasons over category names, never ids** — opaque UUIDs were the easy-wrong signal that let coarse bank labels beat rich history. Each batch is one tool-less structured call whose prompt embeds:\n\n- the active category list **by name + group**, split into income vs expense, no ids,\n- each row's raw description, amount, type, and `sourceCategory`,\n- each row's distilled history context — examples and frequency stats **by category name**, with any dead/archived id dropped rather than shown (the dominant signal stays a real, usable category).\n\nThe prompt leads with the user's own history as the **primary** signal: when the merchant appears in history, the model strongly prefers the category they've used for it (weighing recency, frequency, amount). `sourceCategory` is demoted to a weak hint, and a coarse value like \"Other\" is explicitly framed as *not* a category — never mapped to \"Other Income.\" Each row comes back with a merchant, a category name, and a confidence (`high` for an obvious match, `low` for a reasonable inference).\n\nCode maps each returned name → a `categoryId` within the row's **type-appropriate** categories (case-insensitive exact match; income names resolve only against income categories, expense only against expense — so an income category can't land on an expense). Results are filtered to ids in that batch; a name that matches no type-appropriate category leaves the row uncategorized (it keeps its cleaned merchant and stays re-enrichable). Resolved rows are merged into staging — filling only empty fields, so a re-run never clobbers a hand-mapped or already-landed value. Each landed batch is persisted to `transactions.csv` immediately, not buffered to the end. There is **no auto-retry**: a batch that throws is logged and skipped, its rows left incomplete; the user-initiated re-run is the retry.\n\n**Transfers** participate here too, but only to resolve their counterpart, and only when they carry transfer context and have no counterpart yet. Their batch is a separate call with its own small schema (a heterogeneous task — they get no merchant or category): the transfer rows plus each one's `recentTransfers` / `topAccounts` go in, and `{ id, account, confidence }` comes back per row, where `account` is a counterpart account **name** the model picks from the context (using the description + amount + the recent/periodic pattern) or `\"\"` when unsure. Code maps that name → an `accountId` (case-insensitive against current non-archived accounts), validates it isn't the row's own account, and writes it to `targetAccountId`. An unknown name, an empty pick, or the row's own account leaves the counterpart blank — a wrong or blank answer degrades to \"leave it for the user\", never a bad pairing. Transfers are few, so this is typically one batch; it runs in the same bounded-parallel pool as the category batches.\n\n## Data model\n\nThe staging row (`ImportTransaction`) and the intermediate `StagedRecord` it's built from are specified in `DATA_MODEL.md`. The import-specific invariants:\n\n- **`description` = raw text trimmed to 45 chars**, set in `buildStaged` for both paths. This is the single canonical form used both for matching (history is already trimmed, so matching is trimmed-vs-trimmed consistent) and for `note` at merge. Truncation drops trailing reference-number noise. There is no separate full-raw field.\n- **`merchant` / `accountId` / `categoryId` start empty** — they're populated by grounding and the classifier, never by the source adapter.\n- **`duplicate`** marks a row that matches an existing budget transaction; **`duplicateConfidence`** carries the match tier (`high` from the exact rules, `low` from the relaxed date window) so the preview can flag a speculative dup for review.\n- **History context lives in `context.json`** keyed by row id — ephemeral classifier input, never a `transactions.csv` column and never persisted to `Transaction`.\n- **Confidence** is `high` / `low` — `high` from fast-path and dedup, the model's call otherwise; low rows are flagged in preview.\n\n## Staging & resume\n\n`.capy/import/` is the **single source of truth for resume**. Where the user lands on reopen is a pure function of which artifacts exist — there is no separate crash state.\n\n```\n.capy/import/\n  sources/              # original files (read-only to the engine)\n  transactions.csv      # normalized staging (written from History on)\n  context.json          # per-row history signals (written during History)\n  transfer-context.json # per-transfer-row direction-aware history (written during History)\n  state.json            # phase + metadata\n```\n\n- **No `transactions.csv`** (the run died during Reading / Normalizing / History) → file-attach screen; stale `sources/` is cleared. Those phases are seconds of code — nothing to recover.\n- **`transactions.csv` exists** (died or stopped during Categorizing) → land on the preview with whatever enrichment was written; Enrich is ready (user-initiated, no auto-fire). `start()` notices the existing rows and resumes at Categorizing over the remainder.\n\n**Interrupt, crash-recovery, and resume are one code path.** Stop is a crash you chose — all three leave identical persisted state and resume by re-running idempotent enrichment.\n\n**One predicate gates category enrichment:** `needsEnrich = !duplicate && (!merchant || !categoryId)` (transfers are exempt — they get no merchant/category). It covers fast-pathed rows, duplicates, hand-mapped rows, batches that already landed, and re-runs after a partial — no special cases. The \"import 1000, do it myself\" flow falls straight out: map rows in the UI, those rows fail the predicate, Enrich does only what's left. **Transfers have a sibling gate** for the counterpart: `needsTransferEnrich = transfer && !duplicate && !targetAccountId && hasTransferContext` — idempotent the same way, so once the counterpart is set (by the model or by hand) the row drops out and a re-run never re-asks.\n\n**Stop** (awaitable) requests a clean stop: no new batches dispatch, the in-flight one finishes its write, and the returned promise resolves once no further write is pending — which is what lets **Cancel** safely discard staging afterward. **Cancel** = stop + clear `.capy/import/`. Re-running on staged data is idempotent.\n\n## Errors\n\n- **No transaction data** (a selfie, a non-financial document): the extraction call returns a structured `{ error: \"no_data\" }`. The orchestrator logs *\"No transaction data found,\"* writes no staging, and returns to file-attach. When several files are imported together, a single no-data file is skipped with a warning rather than failing the run; only all-empty is terminal.\n- **Batch failure:** no auto-retry. Logged; the batch's rows stay incomplete (they fail the predicate) and the run moves on. The idempotent re-run is the retry.\n- **All duplicates:** no special handling — every row lands unselected and Merge is disabled until the user selects something.\n\n## Preview & merge\n\nA self-contained module operating on staging only — no budget-data dependency until merge.\n\n- **Live during a run, read-only.** Rows render beneath the progress bar as soon as they exist and fill in live as batches land; the table is read-only while a run is in flight and **Merge is disabled** until it settles. Stop/Enrich live in the progress section's header; the floating action bar carries the selection count, the sum, and **Merge**.\n- **Account mapping** lives in a set of mapping rows — one per imported account, each showing the imported name → a target-account selector (an existing budget account, or create-on-merge — the default), edits applying live. The same rows render in two places: the Map-accounts dialog the table's ACCOUNT cells open (every imported account), and the merge confirmation (only the source accounts with selected rows, so the gate reflects exactly what merges). The ACCOUNT cell shows only the mapped *target* name, with a \"new\" tag when merge would create the account — the raw imported → target relationship is visible in the rows. The merge dialog is an editable safety gate: titled with the transaction count (\"Merge N transactions?\") and subtitled with the total it adds (\"This will add **{total}** to your budget\"), it lists those same source → target rows so a wrong mapping is both visible and correctable before commit — fixing it there flows into the merge. Rows are the account-mapping axis only; a transfer's counterpart is not a mapping and gets no row. **Transfers that lose their counterpart** — the merge couldn't pair them, so they import as income/expense — collapse into a single amber warning line with their count, never enumerated. The confirmation's rows scroll within the dialog body (the selector's dropdown portals out so it isn't clipped); the Map-accounts dialog caps the list at five rows. Either way the Cancel/Merge buttons stay pinned on-screen. Aliases stored in `.capy/aliases.json` survive across imports and pre-populate the mapping; they're persisted at merge only for the sources that actually merged a row, so a fully-deselected source leaks no mapping into the next import.\n- **Run notes** sit in one compact panel above the table: a duplicates line splitting certain matches from possible (close-date) ones — which also carry an amber badge in the table prompting review — and an issues line counting low-confidence and uncategorized rows once the run settles. Duplicates land unselected.\n- **Transfers** show an account dropdown for the counterpart, pre-filled when grounding or the model resolved it. The dropdown stays the standard account list (never reranked — account ordering is consistent app-wide); a row whose counterpart is still unset gets an outline on the dropdown so it's easy to spot in the preview, with the placeholder (\"From account\" for an inflow, \"To account\" for an outflow) as the only wording.\n- **Transaction table** — full CRUD, multi-select (unselect to skip), search, inline editing; the same interaction patterns as the main transactions view. Edits write back to staging.\n- **Merge** converts the selected rows into budget transactions: creates unmapped source accounts, links transfer pairs (same date, opposite amounts, different accounts), and bulk-inserts. The field mapping:\n\n| Staging field | `Transaction` field |\n|---|---|\n| `description` (trimmed) | `note` |\n| `merchant` (cleaned) | `merchant` |\n| `categoryId` | `categoryId` (empty for transfers) |\n| `accountId` (resolved) | `accountId` |\n\n`note` is the trimmed `description`, period. `merchant` on `Transaction` is reserved for the cleaned name — the two are never copied into each other.\n\n## Import log\n\nOn a successful merge, an entry is appended to the import log in the budget folder, recording the source file names, the date, and quick stats (transaction count, accounts created, date range). The drop zone checks new file names against the last 20 entries and shows an advisory amber warning when a name was imported before — the user can still proceed. Merge clears `.capy/import/` after writing the log entry. Capy can read the log for audit queries.\n\n## Custom instructions\n\nThe Import tab carries a per-run hints field (format quirks, a default source account) and persists free-text notes to `import-instructions.md` in the budget folder — separate from the chat overlay's `capy-instructions.md` so import-tuning notes don't bleed into chat behavior. The hints ride into the import session's system prompt, the one channel both stateless calls (mapping / extraction and Categorizing) see.\n\n## Design principles\n\n- The preview is a self-contained module — it reads and writes staging only.\n- The model is a stateless function called at two points; code drives everything between and around them.\n- The main budget data is read-only until merge.\n- All intermediate state lives in `.capy/import/` — no pollution of the budget database during import.\n",
  "INTELLIGENCE.md": "# Intelligence Layer\n\nCapy is an AI financial assistant. The intelligence layer is **provider-pluggable**: the renderer talks to a `CapySession` interface, and a factory selects one of three concrete adapters at runtime — Claude Code CLI, Anthropic API, or OpenAI API — based on user settings. The app is fully functional without intelligence; it's additive.\n\n## Architecture\n\n```\n┌──────────────────────────────────────────────┐\n│  Capy Overlay                                │\n│  Messages, input, multimodal attachments     │\n└──────────────┬───────────────────────────────┘\n               │ CapySession interface\n               ▼\n┌──────────────────────────────────────────────────────────────┐\n│  Session Factory  (provider, config, options)                │\n│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐  │\n│  │ ClaudeCliSession│  │AnthropicSession│  │ OpenAiSession  │  │\n│  │ subprocess+MCP  │  │ in-process loop│  │ in-process loop│  │\n│  └────────┬────────┘  └────────┬───────┘  └────────┬───────┘  │\n└───────────┼────────────────────┼───────────────────┼──────────┘\n            │                    │                   │\n            ▼                    ▼                   ▼\n┌──────────────────────┐  ┌─────────────────────────────────────┐\n│ MCP server           │  │ In-process tool dispatch            │\n│ stdio + node fs      │  │ data + mutation + start_import +    │\n│                      │  │ read_file + read_spec + render      │\n└──────────┬───────────┘  └──────────┬──────────────────────────┘\n           │                         │\n           ▼                         ▼\n        ┌──────────────────────────────┐\n        │  ToolContext                 │\n        │  { repo, fileAdapter, path } │\n        └──────────────┬───────────────┘\n                       ▼\n                   Budget Data\n```\n\nTwo transport models share a single tool layer:\n\n- **Claude Code adapter** spawns the `claude` CLI as a subprocess and routes all tool calls through the MCP server, which uses node `fs`.\n- **API adapters** run the agentic loop in the renderer and dispatch tool calls **in-process**. They use the Tauri `fs` adapter for the same handlers — same `ToolContext` shape, different `FileAdapter` implementation.\n\nThe tool handlers don't know which transport called them.\n\nSmart Import does not run through the agent loop. It's a code-orchestrated pipeline (see `IMPORT.md`) that calls the model statelessly through the `structured()` primitive — no tools, no accumulated context. The chat surface's only connection to import is the `start_import` tool: the chat on-ramp that stages an attachment and hands it to that pipeline.\n\n## Session Interface\n\n`CapySession` defines the contract:\n\n- `send(content)` — send user message (`MessageContent`: plain string or array of text / image / document blocks)\n- `stop()` — interrupt current response (provider-specific: CLI kills the subprocess; API adapters abort the in-flight request)\n- `restart()` — kill session and start fresh\n- `kill()` — terminate\n- `onEvent(callback)` — receive stream events\n\n### Stream Events\n\n| Event | Meaning |\n|---|---|\n| `content` | Full cumulative blocks array for the current user→done cycle (entire agentic loop, across iterations and tool calls). Consumer replaces the trailing assistant message's blocks wholesale on every emit. |\n| `tool-result` | A tool call finished executing. Carries the tool name, the adapter-specific call id, and an `ok` flag. Used by the hook to invalidate caches live, per-call, instead of waiting for `done`. Distinct from the `tool-activity` ContentBlock, which signals the call was *requested*. |\n| `done` | Cycle complete |\n| `error` | Error message |\n\n### Content Blocks\n\n| Type | Data |\n|---|---|\n| `text` | Plain text string |\n| `table` | Headers + rows (amounts get semantic coloring) |\n| `bar-chart` | Title + label/value pairs |\n| `donut-chart` | Title + label/value pairs |\n| `tool-activity` | Tool name (persists in chat history) |\n| `file-attachment` | File name, size, mediaType (rendered as chip) |\n| `followups` | Array of `{label, prompt}` follow-up suggestion chips. Click sends `prompt` as next user message. |\n\nA `BlockRenderer` routes each block to its specialized renderer.\n\n### Streaming Behavior\n\nEvery `content` event carries the **complete cumulative blocks array** for the current user→done cycle — the entire agentic loop, across iterations and tool calls. The consumer (`mergeStreamContent`) replaces the trailing assistant message's blocks wholesale on every tick. Adapters are responsible for accumulating across model turns so non-text blocks (charts, tables, follow-up pills) survive subsequent turns.\n\nAdapter accumulation:\n\n- Anthropic + OpenAI adapters: a `completedBlocks` array lives outside the agentic loop. Each iteration appends a fresh text block (driven by streamed text deltas) and pushes any tool-use blocks (rendered or `tool-activity`). The array survives across tool-result rounds.\n- Claude Code adapter: the CLI emits one `assistant` event per content block, all sharing the message's `id` (the id persists even across in-turn tool boundaries). The decoder is stateless and forwards `message.id` as the optional `messageId` on `StreamEvent.content`; `CycleAccumulator` stitches events into one cumulative array — appending same-id text blocks as distinct blocks (replacing in place only when the incoming text extends the in-progress one, the cumulative-snapshot shape older CLIs streamed), promoting blocks into a finished-turns buffer when `messageId` changes, and dropping text for the rest of the cycle once a rendered followups block lands (see the terminal-signal tool).\n\nAdapters emit `StreamEvent`s directly (`content` / `tool-result` / `done` / `error`) — there's no transport-level event layer above this.\n\nAdapters surface `done` off the model's terminal event (Anthropic `message` / OpenAI `finish_reason`) and abort the transport early rather than waiting for SSE drain — gating on the transport adds seconds of post-content latency. The Claude CLI populates no `stop_reason` on assistant events; its `done` rides the trailing `result` line. The parser still emits `done` early off a terminal `stop_reason` should one appear, with the `result` line as the guaranteed emitter so the UI never hangs.\n\n## Structured Output\n\nAlongside the agentic `CapySession`, the in-process API adapters expose a second, stateless primitive: `StructuredSession.structured(messages, schema, options?) → T`. One constrained model call returns a value the caller's JSON Schema describes — no agent loop, no tools, no accumulated context. An optional `onText` callback switches the call to a streaming request and surfaces the accumulated raw response text per delta — a partial JSON prefix the caller can derive live progress from; the resolved value is identical either way. Smart Import is built on this; the agent loop is for chat.\n\nBoth providers constrain generation to the schema server-side (Anthropic `output_config.format`, OpenAI `response_format` json_schema). `parseStructured` is the client-side enforcement layer: it parses the returned text and validates it against the same schema, so a malformed or off-schema response throws (`SchemaValidationError`) at the call site rather than landing as a silently-wrong object downstream. User turns may carry multimodal content (receipt images, PDF bytes); assistant turns are text-only, because the API rejects image/document blocks in an assistant turn.\n\nThe Anthropic and OpenAI adapters implement both `CapySession` and `StructuredSession`. The Claude Code CLI adapter implements only `CapySession` — its structured-call path is not available, which is why import is gated to the API providers (see **Settings** and `IMPORT.md`).\n\n## Adapters\n\n### Claude Code adapter\n\nSpawns `claude` via Tauri's shell plugin in pipe mode with stream-json I/O on both ends. CLI flags supplied at spawn:\n\n- `--session-id <uuid>` — conversation context\n- `--mcp-config <path>` — points to MCP server\n- `--allowedTools \"mcp__capy__*,Read\"` — allowlist MCP tools + file reading\n- `--disallowedTools \"TodoWrite,Task,Bash,Edit,Write,Glob,Grep,WebFetch,WebSearch,NotebookEdit,KillBash,BashOutput\"` — explicitly block the CLI's stock built-ins. Even when omitted from the allowlist the model still knows they exist and the CLI's baked-in system prompt nudges it to deliberate about them (\"should I use TodoWrite for this?\") — disallowing silences that meta-narration.\n- `--add-dir <budget-path>` — grant Read access to the budget folder\n- `--setting-sources \"\"` — skip CLAUDE.md files\n- env `ENABLE_TOOL_SEARCH=false` — disables the CLI's deferred MCP tool loading (undocumented knob) so every `mcp__capy__*` schema loads upfront. With deferral on, the model must fetch schemas via ToolSearch mid-turn and sometimes calls render tools blind with invented payloads.\n\nLifecycle: spawn lazily on first message, fresh session ID per spawn, process survives overlay close/reopen, `kill()` ends it. Stop / restart recovery (serialize prior conversation, prepend `[Previous conversation]` on next send) is unique to this adapter — API adapters get a simpler abort-and-continue model.\n\n### Anthropic adapter\n\nDirect `@anthropic-ai/sdk` calls from the renderer. The Tauri webview is a real browser; the SDK runs with `dangerouslyAllowBrowser: true` since the key is the user's own and lives on disk.\n\nThe agentic loop owns message history and an `AbortController`. Tool calls dispatch in-process. PDFs ride through the SDK's native `document` content type.\n\n`stop()` aborts the in-flight stream and drops any trailing assistant turn with unmatched `tool_use` blocks (the API would 400 on the next request otherwise).\n\n### OpenAI adapter\n\nUses `chat.completions.create` with `stream: true` — the stable, broadly-compatible endpoint across the GPT-4 family, GPT-4o, GPT-4.1, GPT-5.\n\nNotable protocol deltas vs Anthropic:\n\n- Tools wrapped as `{ type: \"function\", function: {...} }`.\n- Tool calls stream as deltas keyed by `index`; arguments arrive as a JSON string sliced across many chunks. A per-index accumulator collects fragments; `JSON.parse` runs only after the stream finishes.\n- Tool results appended as `{ role: \"tool\", tool_call_id, content }` messages.\n- Images convert to `{ type: \"image_url\", image_url: { url: \"data:...\" } }`.\n- System prompt is a `{ role: \"system\" }` message at the head of each request.\n- `delta.content` is non-cumulative — accumulated locally before emit.\n\nPDFs aren't supported on `chat.completions`. The adapter drops any `document` block and substitutes an explanatory text note describing what happened, so the model can respond coherently (e.g. ask the user to paste contents or share a screenshot). Callers attach PDF blocks uniformly across providers; provider divergence is the adapter's responsibility.\n\nAll three adapters share `buildRenderToolMap()` from `@capybudget/intelligence` for the render-tool → ContentBlock contract. Adding a new render tool means defining it once in `RENDER_TOOL_DEFS` plus its mapping in `render-map.ts`; the three adapters pick it up automatically.\n\n## Tool Layer\n\nSingle source of truth shared between transports:\n\n- **Definitions** — tool descriptors (name, description, JSON-Schema input). `getToolDefinitions()` is the single source: no argument, one surface. The MCP server, the in-process chat agent loop, and external agents all see the same set. The structured import session calls the model with no tools, so it never draws from here.\n- **Dispatch** — `runTool(name, input, ctx) → string`. The MCP server and the API adapters call this with the same signature. `ToolContext` is `{ repo, fileAdapter, budgetPath, attachments?, importSupported?, pdfSupported? }` — the last three ride only the chat path, for `start_import`.\n- **Handlers** — per-tool implementations:\n  - **Data tools** — `list_accounts`, `list_transactions` (filters + `sort` + `offset` + `format: \"compact\" | \"full\"`, plus `ids` to fetch exact rows after a scan), `search_transactions` (fuzzy cross-field + money query and structured filters → compact rows), `group_transactions` (the universal aggregator: same filters as search, then `groupBy` one or more dimensions — merchant/category/account/type/month/week/dayOfMonth/amountBucket — and request `metrics` over signed cents, including per-group `cadence` for recurrence; subsumes spending-by-category, merchant rollups, duplicate clusters, and interval analysis), `list_categories`\n  - **Mutation tools** — full CRUD for transactions / accounts / categories, plus `bulk_update_transactions` (category/account/date/merchant across many rows; skips transfers for category/account/merchant). `update_account` carries `archived` (archiving fails on a non-zero balance) and `excludeFromNetWorth`. `update_category` carries `archived` and `budgetCents` (the explicit `assigned` budget — `null` untracked, `0` tracked-at-zero, omitted unchanged; a category without one still has an implicit target derived from its spending history).\n  - **start_import** — the chat on-ramp into Smart Import. Takes no arguments: it stages the files attached to the in-flight chat turn into `.capy/import/sources/` and marks the run so the Import screen auto-starts the orchestrator. Capy calls this for any uploaded file instead of reading it and creating transactions itself. See **Import On-Ramp** below.\n  - **read_file** — generic budget-folder text reader; mirrors what Claude CLI's built-in `Read` provides natively\n  - **read_spec** — reads one of the app's design docs (`specs/*.md`). Content is bundled at build time into `specs.generated.ts` — no filesystem access, no path resolution surface. Use when capy needs implementation detail beyond what the system prompt already embeds.\n  - **Render tools** — dispatch validates the payload against the render-map builders: valid calls return `\"Rendered.\"`, malformed or empty-data input returns an error result. The frontend intercepts the `tool_use` event and emits the corresponding ContentBlock. See **Render tools** below.\n\nAll filesystem access goes through the `FileAdapter` on the context, so the same handler runs against node fs (MCP server) and Tauri fs (API adapters in the renderer). The `FileAdapter` interface covers core CSV repo ops (read/write/rename/join) plus the staging-handler ops (`mkdir`, `exists`, `readDir`, `appendFile`, `remove`, `stat`) the import pipeline and `start_import` use.\n\n### Prompt caching\n\nThe tools + system prefix is static across a session, so the API adapters cache it instead of re-billing ~7-8K tokens of schema every turn of a multi-turn loop. All per-turn content (the context wrapper, budget snapshot, attachments — see **Context Enrichment**) rides in the user messages, after the prefix, so the prefix stays identical turn-to-turn.\n\n- **Anthropic** marks the system block with `cache_control: { type: \"ephemeral\" }`. One breakpoint at the end of system caches everything before it in the prefix hierarchy — tools, then system. Cache hits show up as `cache_read_input_tokens` in usage from turn 2 on.\n- **OpenAI** caches eligible prefixes (>~1024 tokens) automatically, no flag — the adapter's job is to keep the prefix byte-stable: the system message is immutable for the session and leads every request, tools follow the same definition order, dynamic content never bakes into either.\n\nThis is in-process-adapter only; the Claude CLI manages its own caching.\n\n### Mutation cache invalidation\n\nThe app invalidates caches per mutation tool call (not per turn) so the UI reflects Capy's changes live as it works. `MUTATION_TOOL_NAMES` is exposed for matching.\n\n### Render tools\n\n| Tool | Input | Renders as |\n|---|---|---|\n| `render_table` | `{ headers, rows }` | Data table with amount coloring |\n| `render_chart` | `{ title, type: \"bar\" \\| \"donut\", data: [{label, value}] }` | Horizontal bar chart or SVG donut chart with legend, per `type` |\n| `render_followups` | `{ chips: [{label, prompt}] }` (1–4 items) | Follow-up suggestion chips after an answer. |\n\nDispatch validates the payload with the same rules the frontend renderer applies — malformed or empty-data input returns an error result so the model corrects itself and retries. Valid calls are otherwise no-ops on the dispatch side; they carry structured data from AI to frontend via `tool_use` events.\n\n`render_followups` is a **terminal-signal tool**: when the model calls it successfully, the assistant turn is over. The API adapters dispatch the call (so the UI gets the chips) and push the tool_result to history, then exit the agentic loop without making another request. A followups call that fails validation is not terminal — the loop continues so the model sees the error result and recovers. Skipping the ack round-trip saves a full stream per turn. The Claude CLI runs the loop in-subprocess; the prompt tells the model to stay silent after calling `render_followups`, and the accumulator drops any text it still emits for the rest of the cycle. The drop latch arms only on a successfully rendered followups block — after a failed render the model's recovery text still shows.\n\n## MCP Server (External Agents)\n\nThe MCP server is a thin transport: it wires the tool definitions to ListTools and `runTool()` to CallTool. It exposes `getToolDefinitions()` — the one shared surface — to external agents (Claude Desktop / Cursor / VS Code Copilot). The in-process chat agent loop sees the same surface; dispatch behavior is identical across both. (`start_import` is a no-op from MCP — it needs the chat turn's attachments, which an external agent's call doesn't carry, so it returns guidance rather than staging.)\n\n```json\n{\n  \"mcpServers\": {\n    \"capybudget\": {\n      \"command\": \"npx\",\n      \"args\": [\"@capybudget/mcp\"],\n      \"env\": { \"BUDGET_PATH\": \"/path/to/budget\" }\n    }\n  }\n}\n```\n\n`immediate: true` on the repository — writes flush to disk before returning tool results. SIGTERM/SIGINT call `repo.dispose()` for graceful shutdown.\n\n## Settings\n\nUser-facing config persists via `@tauri-apps/plugin-store` (file-based, in the app config directory):\n\n```ts\ninterface IntelligenceConfig {\n  provider: \"claude-cli\" | \"anthropic\" | \"openai\" | null\n  anthropic: { apiKey: string; model: string }\n  openai:    { apiKey: string; model: string }\n  claudeCli: { model: string } // \"\" lets the CLI pick its default\n}\n```\n\nSettings lives in the `/budget` Intelligence section. It renders a provider radio + per-provider config (API key where applicable, model picker, test-connection button). The radio order is **Off / Anthropic API / OpenAI API / Claude Code**; Claude Code carries an `advanced` badge and sits last as the source-build option. First-run defaults to `null` — users must explicitly pick a provider so they're never surprised by quota usage. The radio's \"Off\" label maps to `null` at the form boundary. Claude Code is auto-detected via `claude --version` and disabled in the picker if not installed.\n\nEvery provider uses one shared model picker: a curated dropdown plus a \"Use a custom model\" toggle that swaps in a free-text field for any model ID outside the list. A saved model not in the curated list opens the field in custom mode, so a user's pinned model survives a refreshed list. The curated options:\n\n- **Anthropic** — Claude Opus 4.8 (`claude-opus-4-8`), Claude Sonnet 4.6 (`claude-sonnet-4-6`, the default), Claude Haiku 4.5 (`claude-haiku-4-5`).\n- **OpenAI** — GPT-5.5 (`gpt-5.5`, the default), GPT-5.4 mini (`gpt-5.4-mini`), GPT-5.4 nano (`gpt-5.4-nano`).\n- **Claude Code** — Default (empty, the CLI decides), plus the `opus` / `sonnet` / `haiku` aliases. A non-empty value passes through as the CLI's `--model` flag at spawn; empty omits the flag.\n\nWhen `provider === null`, the Capy overlay shows an empty-state CTA instead of the chat UI.\n\nSmart Import needs the structured-output primitive, which only the Anthropic and OpenAI adapters implement. `canImport(provider)` is the single gate — true for `anthropic` and `openai`, false for `claude-cli` and `null`. The Import tab shows an offline CTA when it's false, and the chat `start_import` tool returns switch-provider guidance.\n\nThe Intelligence section also hosts a chat-instructions editor for `capy-instructions.md` (see Custom Instructions). The same file is editable from the Capy overlay; edits apply to the next conversation. The web demo can't store an AI provider, so it renders the provider list disabled behind a desktop-only notice and omits the per-provider config and the chat-instructions editor; Categories management stays fully functional.\n\n## Context Enrichment\n\nEach user message is wrapped with app context before sending. The first message of a session also carries a compact **budget snapshot** — the budget's currency, account and transaction counts, the date range, the category list, and (only when present) the foreign-currency accounts — so Capy knows the shape of the data (and the currency to format in) without a tool round-trip.\n\n```\n[Context]\nBudget: personal\nDate: March 15, 2026\nBudget folder: /path/to/budget\n\n[Budget snapshot]\nCurrency: EUR\nAccounts: 4 active\nTransactions: 1820 (2020-01-03 → 2026-03-14)\nForeign-currency accounts:\n  Tinkoff: RUB\nAggregate totals (group_transactions, list_accounts balances) are in the default currency (EUR); per-row amounts in list_transactions are each account's own currency.\nCategories:\n  Income: Paycheck, Other Income\n  Fixed: Housing, Bills & Utilities, Subscriptions\n  Daily Living: Groceries, Dining Out, Transportation\n  ...\n\n[User message]\nWhat did I spend on food this month?\n```\n\nThe foreign-account block and its roll-up note appear only once an account holds a non-default currency; a single-currency budget's snapshot is unchanged.\n\nThe budget's currency is display-only (money stays integer minor units everywhere — see `DATA_MODEL.md`). Only the currency code reaches the model — the user's UI format overrides (decimals, symbol position) stay in the app and never thread into Capy. It threads three ways: into the snapshot above, into the chat system prompt's money examples, and into the `ToolContext` (so money in tool results is rendered correctly). Aggregate roll-ups are converted into the budget default: `list_accounts` balances and every `group_transactions` total. Per-row amounts in `list_transactions` are rendered in **each account's own currency** — the native truth of the row — since totals are the aggregator's job, not the row list's. Each currency uses its own default convention, not the user's overrides. The tool handlers receive currency on the context, not from the repository, which exposes only entities.\n\n## Custom Instructions\n\nUsers can write custom instructions for the chat assistant in `capy-instructions.md` in the budget folder, edited from either the Capy overlay dialog or the Settings ▸ Intelligence editor — both write the same file. These compose into the chat system prompt at session start:\n\n```\n{SYSTEM_PROMPT}\n\n## User instructions\n{contents of capy-instructions.md}\n```\n\nImport sessions read a separate `import-instructions.md` from the same folder — the chat-tuning instructions (\"answer in fewer words\", \"always show charts\") rarely overlap with import-tuning instructions (\"treat ATM withdrawals as transfers to cash\"), so the two surfaces are kept distinct. Each file is user-provided and takes effect on the next session of its respective surface.\n\n## Custom Commands\n\nQuick command templates stored as `capy-commands.json` in the budget folder. Three defaults provided (spending breakdown, subscriptions audit, savings rate). Sorted alphabetically.\n\n## System Prompt\n\nEstablishes Capy's personality:\n\n- Financial assistant with full CRUD capabilities\n- Takes action directly — never tells user to do things in the UI\n- Uses render tools for all structured output (tables, charts)\n- Defaults to current month when no date range specified\n- Concise, direct answers\n- Confirms destructive actions before executing\n\nThe chat prompt opens with a shared **app-knowledge brief** (`specs/APP_KNOWLEDGE.md`, factored through `prompts/app-knowledge.ts`) — the transaction log, accounts, categories, derived budgets, the analytics surface, who the user is, and what Capy's job is. It's a tight, purpose-built doc, not a spec dump: ~1.3K tokens of always-on common ground rather than the full schema and feature inventory.\n\nThe brief is sourced from `packages/intelligence/src/specs.generated.ts`, regenerated on every build by `scripts/generate-specs.ts`. For detail beyond the brief — the exact CSV schemas in `DATA_MODEL.md`, the full feature inventory in `PRODUCT.md`, the architecture, the import pipeline, the intelligence layer itself — capy calls `read_spec`.\n\n## Import Sessions\n\nSmart Import is a code-orchestrated pipeline (`IMPORT.md`), not an agent session. It calls the model through `structured()` at two points, each a single constrained call with no tools and no accumulated context:\n\n- **Mapping / extraction** (Normalizing) — a CSV's headers + samples become a `CsvMapping`; an image or PDF's bytes become the same intermediate transaction records directly. Receipt images ride as base64 `image` content, PDFs as base64 `document` content, on the user turn the structured call sends.\n- **Categorizing** — batches of ~25 rows, each carrying its distilled history context and the category list, return `{ id, merchant, categoryId, confidence }[]`. Batches are bounded-parallel and fail in isolation.\n\nAll these calls share one short import system prompt (`import/system-prompt.ts`): it sets the role and two invariants — extract only what the source contains, and answer with the requested structure — while each call's task and schema ride in the request itself. The per-run hints and `import-instructions.md` from the Import tab compose onto this prompt. There is no per-call tool surface, no agent loop, and no `categoryConfidence` REPL between AI and tools; confidence is just a field each Categorizing row returns (`high` / `low`), which the preview renders as a dot.\n\n## Session Tool-Call Budget\n\nEvery `CapySession` enforces a per-session cap of **100 tool calls** as a runaway-loop backstop. When exceeded, the session emits a `StreamEvent.error` describing the budget exhaustion and terminates that turn cleanly. The user sees the error and can run again; the budget resets per session.\n\nThis is a hard backstop on the chat agent loop, not the normal path. A well-formed answer converges in a handful of calls. The cap exists to bound the failure mode when something goes wrong. (Smart Import is not an agent loop — its stateless `structured()` calls make no tool calls and don't draw on this budget.)\n\nEnforcement varies by adapter:\n\n- **Anthropic / OpenAI** count tool calls inline as they dispatch them. When the cap trips mid-turn, remaining tool_use blocks in the same turn receive a budget-exhausted error result (so the API doesn't see dangling tool_use) and the agentic loop exits without making the next request.\n- **Claude CLI** parses each assistant event, dedups tool_use IDs into a Set, and kills the subprocess + surfaces the error event when the Set grows past the cap. The CLI can't be told to stop from outside, so termination is the cleanest signal we can give.\n\n## Per-provider Stop / Restart\n\n| | Claude CLI | Anthropic + OpenAI |\n|---|---|---|\n| `stop()` | kill subprocess + new session ID; serialize prior chat and prepend on next send | abort in-flight request; drop trailing assistant turn with unmatched tool calls; messages otherwise intact |\n| `restart()` | kill + new session ID + clear recovery state | abort + clear messages |\n| Recovery on next send | `[Previous conversation]` block prepended | continues normally |\n\nThe recovery dance is unique to the CLI (it can't reliably resume a session interrupted mid-turn). API adapters get a clean model because `AbortController` doesn't leave them in an ambiguous state.\n",
  "MONOREPO.md": "# Monorepo Structure\n\nnpm workspaces monorepo. Shared logic lives in packages. Deployment targets are thin shells that mount the shared React application with platform-specific adapters.\n\n## Packages\n\n| Package | Name | Purpose |\n|---|---|---|\n| `packages/core` | `@capybudget/core` | Types, money utilities, pure entity service functions |\n| `packages/persistence` | `@capybudget/persistence` | Repository interface, file adapter, CSV implementation |\n| `packages/intelligence` | `@capybudget/intelligence` | Session interface, stream events, content blocks, system prompt |\n| `packages/app` | `@capybudget/app` | Full React application — components, hooks, stores, routes |\n| `packages/mcp` | `@capybudget/mcp` | Standalone MCP server for any AI agent |\n\n## Shells\n\nThin deployment targets. Each provides platform adapters and mounts `<App />` from `@capybudget/app`.\n\n| Shell | Location | Purpose |\n|---|---|---|\n| Desktop | `src/` + `src-tauri/` | Native Tauri app with local file I/O and Claude CLI |\n| Demo | `apps/demo/` | Browser-based demo with preset data (demo.capybudget.app) |\n| Website | `apps/www/` | Promo site — Astro 6 static, deployed to Vercel (capybudget.app) |\n\n## Dependency Graph\n\n```\n              core\n           ↗   ↑   ↖\n  persistence ←─ intelligence\n       ↑  ↖     ↗  ↑\n       │    app     │\n       │   ↗   ↖   │\n      desktop  demo │\n                    │\n          mcp ──────┘\n```\n\nCore depends on nothing. `intelligence` depends on `persistence` so\nthe in-process tool dispatch (used by API-adapter sessions and re-used\nby the MCP server) can take a `BudgetRepository` + `FileAdapter`.\nNo circular dependencies.\n\n## Adapter Pattern\n\nPlatform decoupling through three injected interfaces:\n\n**FileAdapter** (`@capybudget/persistence`) — file read/write/rename/join. Desktop: Tauri plugin-fs. Demo: not used (in-memory repository).\n\n**CapySession** (`@capybudget/intelligence`) — AI session lifecycle. Desktop: Claude CLI subprocess via Tauri shell. Demo: stub prompting local install.\n\n**BudgetService** — budget detection and bootstrap. Desktop: Tauri fs + dialog. Demo: preset data loader.\n\nShells inject adapters via React context providers from `@capybudget/app`.\n\n## What Lives Where\n\n**`@capybudget/core`** — domain types (Account, Category, Transaction and unions), money utilities, pure entity services (CRUD for accounts, categories, transactions), bulk operations, merchant matching. Zero platform dependencies.\n\n**`@capybudget/persistence`** — `BudgetRepository` interface, `FileAdapter` interface, `CsvRepository` implementation, CSV parsing with typed coercion, debounced writer. Depends on core for types. The `BudgetRepository` interface is the extension point for future storage backends (database, etc.). `FileAdapter` is specific to the CSV implementation.\n\n**`@capybudget/intelligence`** — `CapySession` interface, stream event types, content block types, system prompt template, context builder, tool definitions and in-process dispatch (`runTool`), provider config types, and the `createIntelligenceSession` factory. The provider session adapters (`AnthropicSession`, `OpenAiSession`) live in `intelligence/src/adapters/` and are exported via the `@capybudget/intelligence/adapters` subpath. Depends on core (types) and persistence (`BudgetRepository` + `FileAdapter` for tool dispatch). See `INTELLIGENCE.md`.\n\n**`@capybudget/app`** — all React components (budget UI, capy overlay, shadcn primitives), TanStack Query/Router hooks, Zustand stores, routes, context providers for dependency injection. Depends on core, persistence, intelligence.\n\n**`@capybudget/mcp`** — standalone MCP server. Thin transport wrapper around `runTool` for data, mutation, and render tools. Continues to own the import + CSV tool handlers locally (refactored to `FileAdapter` in Phase B). Depends on core, persistence, and intelligence. See `INTELLIGENCE.md`.\n\n## Import Convention\n\n- `@capybudget/*` for shared packages\n- `@/` alias for app-internal imports within `packages/app`\n- kebab-case file naming throughout\n",
  "PRODUCT.md": "# Product Vision\n\n**Capy Budget** is a self-contained desktop app for tracking personal finances across every account you own — wallet, checking, savings, crypto, gold pile.\n\n## Philosophy\n\n- **No cloud, no subscription.** Your data lives in plain CSV files in a folder you choose.\n- **No opinionated methodology.** The app tracks what happened — you decide how to budget.\n- **Budgets handle themselves.** Targets derive from your own spending history, so a budget exists from day one with nothing to assign. Setting an explicit number is a quality-of-life override, not a mandatory grind.\n- **Sync for free.** Point the data folder at iCloud or Dropbox and get cross-device sync without a backend.\n- **Self-describing data.** Metadata lives alongside data files. Copy a folder to another machine and it just works.\n- **Intelligence is optional.** An optional layer (Capy) adds smart import, auto-categorization, and natural language insights — pluggable across Claude Code CLI, Anthropic API, and OpenAI API. The app is fully functional without it.\n\n## Core Principle\n\nEvery feature derives from a simple, well-maintained transaction database. The complexity of maintaining that database is where intelligence makes the difference.\n\n## Key Features\n\n### Accounts\n\nAccounts represent financial entities: bank accounts, wallets, credit cards, loans, investments. See DATA_MODEL.md for types, schemas, and integrity rules.\n\nBalances are always derived from transactions — never stored. Net worth is the sum of all non-archived account balances (assets positive, liabilities negative).\n\nAccounts can be archived (zero balance required) or deleted (only if no real transaction history).\n\n### Multi-currency\n\nEach account holds one currency, chosen at creation and locked once it has transactions (re-denominating historical amounts is meaningless — make a new account instead). The budget has a default currency that every roll-up reports in; an account left unspecified takes that default.\n\nMoney is stored native and never re-rated. Conversion happens only at read time, with two rates for two questions: **flows** — what was spent or earned — are valued at the rate stamped on the day each transaction happened, so history never moves as rates drift; **holdings** — what an account is worth now — are valued at today's rate. Rates resolve per currency from a manual override, else a bundled rate table, else 1.0. Cross-currency transfers carry a second amount field — one per leg — so moving money between currencies records the real rate on both sides.\n\nThe two rates don't reconcile, and the gap is real: a foreign balance's spot value (today's rate) minus its cost value (stamped flows) is unrealized FX gain or loss, surfaced as a **net-worth FX delta** rather than hidden. A single-currency budget has every rate at 1.0, so conversion is the identity and none of the multi-currency surfaces appear — no per-currency settings, no FX line, no extra transfer field.\n\nPer-row amounts render in each account's own currency — the native truth of the row — so a foreign transaction shows under its own symbol, never the default's. A single account's page leads with its native balance in its own currency; for a foreign account it also shows the default-currency equivalent at today's rate as a secondary line. Aggregates across accounts (net worth, category and cash-flow roll-ups) report in the default.\n\n### Transactions\n\nThree types: expense, income, and transfer. Amounts are signed (negative = outflow, positive = inflow) with semantic coloring in the UI.\n\n**Credit card lifecycle**\n1. **Purchase**: expense on the credit card. Balance goes more negative. Category impacted now.\n2. **Payment**: transfer from checking to credit card. No category impact. The payment is debt settlement, not spending.\n\n### Categories\n\nFully user-manageable with sensible defaults. Organized into groups that provide financial reasoning: Fixed (hard to change), Daily Living & Personal (where to cut first), Irregular (easy to forget). Users can create, rename, and reorder both categories and groups.\n\nBy default transaction category is null, in the UI this surfaces as 'Uncategorized' and I should be able to reset category value back to null, choosing 'Uncategorized'.\n\n### Monthly Budget\n\nEvery category budgets itself. The Monthly Budget tab tracks this month's spend per category against a target — and that target is set for you, derived from the category's own history (the heavier of last month and a reference average of its active months). Spending alone produces a useful budget: open the tab and every category already has a target and a live progress bar, with no number to assign.\n\nYou choose what \"normal\" the reference measures against. A dropdown on the bar legend sets the **comparison basis** for the whole budget: your recent rhythm over the last 3, 6, or 12 months, or — for categories with a yearly shape like Travel or gifts — the same month a year ago, so a seasonal target compares against the season rather than the quiet months either side of it. Switch it and every target re-derives at once. The default is the trailing three months.\n\nAn explicit budget is an optional override, not a prerequisite. Set one on any category to track against your own figure instead of the inferred one; the rest keep budgeting themselves. A category with no recent spending stays calm and untargeted rather than guessing. Budgets self-tune as spending shifts, so the tab keeps reflecting how money actually moves without periodic upkeep.\n\n### Sidebar\n\nAccounts grouped by type: Cash, Checking, Savings, Credit, Investment, Loans, Archived. Each group shows a subtotal; each account shows its live derived balance.\n\n### Intelligence Layer (Capy)\n\n- **Chat panel** — right-edge slide-out with conversation, tool activity, charts, and follow-up suggestions. See `FRONTEND_DESIGN.md` for the interaction model.\n- **Smart import** — paste a bank screenshot or CSV, Capy parses it, you review and confirm.\n- **Auto-categorization** — learns from existing spending patterns.\n- **Insights** — natural language queries about spending.\n- **Anomaly detection** — flags unusual transactions or spending spikes.\n\n## UX & Visual Design\n\nSee `FRONTEND_DESIGN.md` for interaction patterns, color scheme, typography, and accessibility guidelines.\n",
  "STRUCTURE.md": "# Code Structure\n\nWhere code goes. `MONOREPO.md` defines the dependency graph (acyclic, no upward\nimports); this file governs the finer grain — package internals, file\nplacement, decomposition. When unsure where something belongs, look here.\n\n## Package Layout\n\nA package's `src` stays flat until ~12 files or two distinct domains, then\nsplits into domain subfolders. The `index.ts` barrel is the only public\nsurface: consumers import from `@capybudget/<pkg>`, never a deep path; internal\nreferences are relative.\n\n`core` groups by domain:\n\n| Subfolder | Holds |\n|---|---|\n| `entities/` | Account, Category, Transaction services + domain types, bulk ops |\n| `import/` | CSV mapping, transform, validation, duplicate detection, merge |\n| `analytics/` | Aggregations, time-series, queries |\n| `utils/` | money, dates, merchant matching |\n| `constants/` | Default categories, label/order tables |\n\n`persistence` and `mcp` stay flat (small, single-domain). `intelligence` groups\nunder `prompts/`, `tools/`, `tools/handlers/`, and `adapters/` (provider\nsession implementations).\n\n## Placement Law\n\nA file lives in the lowest layer that can own it. The litmus is *what it\nimports*, not *who uses it*.\n\n| Code | Home | Litmus |\n|---|---|---|\n| Pure domain logic, transforms, queries | `core` | imports only types / other core |\n| Storage, CSV I/O, repository | `persistence` | knows files, not UI |\n| AI provider adapters, sessions | `intelligence/adapters` | imports only `intelligence` types |\n| Tool schemas + dispatch | `intelligence/tools` | one file per domain, mirrors handlers |\n| React data-to-state bridges | `app/hooks` | delegates to core, no business logic |\n| Display | `app/components` | no file I/O, no business logic |\n| Platform / Tauri glue | shell `src/` | imports `@tauri-apps/*` |\n\nA file that imports only `intelligence` types belongs there, not in `app`:\nprovider sessions live in `intelligence/adapters` so any consumer (the app, the\nMCP server, a CLI) reuses them without depending on `app`, and the app keeps\nonly the React context wrapper that injects them. Schema migrations are a\npersistence concern — `persistence` owns the transform, the shell calls\n`repo.migrate()`.\n\n## Components\n\n- A component targets under ~250 lines; past ~400, look for seams. Length is a\n  smell, not a limit — a cohesive container (a shell, a single-state form) may\n  run long. Split where the seams are, not to hit a number.\n- Extract an inline subcomponent to a sibling file once it owns state or is\n  reused; push a pure helper down a layer (`lib`, or `core` if domain-pure)\n  rather than nesting it.\n- Components group by feature (`budget/`, `capy/`, `import/`, `settings/`): a\n  feature owns its components and local helpers, shared primitives live in\n  `components/ui/`, and a component spanning several files gets its own\n  subfolder.\n\n## The `app/lib` boundary\n\n`app/lib` holds app-only cross-cutting UI utilities — class merging,\ntext/number formatting, theming. Not business logic (that is `core`), not\nfixtures or demo data (that is test infrastructure). A file that would compile\nand pass tests with zero React or DOM imports belongs in `core`.\n\n## Tests\n\n- Colocate unit tests as `<source>.test.ts`; full-app journey tests live in\n  `app/src/test/journeys/`.\n- Shared builders (`makeAccount`, `makeCategory`, `makeTransaction`) live once\n  in `core/src/test-factories.ts` (export-only, not a test file) and are\n  imported everywhere — packages do not redefine them. Persistence's CSV-string\n  builders are the exception.\n- A test splits when its module splits; one bloated only by exhaustive edge\n  cases (parsing, stream decoding) may split by concern.\n\n## File Naming\n\nkebab-case throughout (`budget-shell.tsx`, `csv-repository.ts`). Import aliases\n(`@capybudget/*`, `@/`) are in `ARCHITECTURE.md`.\n",
  "TESTING.md": "# Testing\n\n## Stack\n\n| Tool | Purpose |\n|------|---------|\n| Vitest | Test runner (shares `vite.config.ts` for aliases and transforms) |\n| @testing-library/react | Component rendering and queries for journey tests |\n| @testing-library/user-event | Simulated user interactions (click, type, keyboard) |\n| jsdom | Browser environment for component tests |\n\n## Test Types\n\n### Unit Tests\n\nColocated with source files as `*.test.ts`. Test pure functions, services, stores, and repositories in isolation.\n\n```\npackages/core/src/money.test.ts\npackages/app/src/stores/undo-store.test.ts\npackages/persistence/src/csv-repository.test.ts\n```\n\n### User Journey Tests\n\nFull-app integration tests in `packages/app/src/test/journeys/`. Mount the entire component tree with a memory router and in-memory repository — no file I/O, no Tauri APIs.\n\n```\npackages/app/src/test/journeys/account-lifecycle.test.tsx\npackages/app/src/test/journeys/transaction-lifecycle.test.tsx\npackages/app/src/test/journeys/lifted-layout.test.tsx\npackages/app/src/test/journeys/fresh-start.test.tsx\n```\n\nJourney tests exercise real user interactions (open form, type, click, submit) against the complete component tree including routing, TanStack Query, mutations, and undo/redo. Each file pays a one-time cold-start to transform and import the full app module graph, so they run as a dedicated `full-app` Vitest project (`vite.config.ts`) with `pool: \"threads\"` and `isolate: false` — sharing the module graph within the worker keeps the suite fast. The partition is by **cost-class, not directory**: a full-app-mount test colocated beside its component (e.g. `components/budget/first-run-guide.test.tsx`) belongs here too, not just files under `journeys/`. The list lives in `fullAppMountTests` in `vite.config.ts`. Eligibility is stricter than \"mounts the full app\", though: because `isolate: false` shares one module registry across the worker, a file here must **not `vi.mock` a module that another file in the pool uses for real**, and must reset its own state per test (`renderApp`'s `afterEach`, or a `beforeEach` mock/store reset). `budget-selector.test.tsx` is excluded for exactly this reason — it module-mocks `src/services/budget`, which the journey tests exercise live, so a shared registry would feed them the mock and they'd never render; it stays in the isolated `unit` pool. The rest of the suite also runs in `unit` on defaults, since `isolate: false` would corrupt it. A 30s per-test timeout gives headroom over the cold-start under CPU contention.\n\n## Test Infrastructure\n\n| File | Purpose |\n|------|---------|\n| `packages/app/src/test/setup.ts` | Global setup: jest-dom matchers, browser API polyfills, Tauri API mocks |\n| `packages/app/src/test/factories.ts` | `makeAccount()`, `makeCategory()`, `makeTransaction()` — test data builders for app-level tests |\n| `@capybudget/persistence` (`createInMemoryRepository`) | `DisposableRepository` backed by plain arrays (no persistence) |\n| `packages/app/src/test/render-app.tsx` | `renderApp()` — mounts full app with memory router, fresh QueryClient, cleanup |\n| `packages/app/src/test/journeys/setup.ts` | Mocks `createCsvRepository` → memory repo (import in journey tests only) |\n\nFor shared test builders across packages (so `core`, `persistence`, and\n`intelligence` tests don't each redefine their own), see `STRUCTURE.md` § Tests.\n\n### Writing a Journey Test\n\n```typescript\nimport \"@/test/journeys/setup\";\nimport { screen, waitFor, within } from \"@testing-library/react\";\nimport { renderApp } from \"@/test/render-app\";\nimport { makeAccount, makeCategory, makeTransaction } from \"@/test/factories\";\n\nit(\"does the thing\", async () => {\n  const { user, repo } = await renderApp({\n    seed: {\n      accounts: [makeAccount({ name: \"Checking\" })],\n      categories: [makeCategory({ name: \"Groceries\" })],\n      transactions: [],\n    },\n  });\n\n  // Wait for the app to load\n  await waitFor(() => {\n    expect(screen.getByRole(\"heading\", { name: \"All Accounts\" })).toBeInTheDocument();\n  });\n\n  // Interact\n  await user.click(screen.getByRole(\"button\", { name: /add transaction/i }));\n  await user.type(screen.getByPlaceholderText(\"0.00\"), \"42\");\n  await user.keyboard(\"{Enter}\");\n\n  // Assert UI\n  const table = screen.getByRole(\"table\");\n  expect(within(table).getByText(\"-$42.00\")).toBeInTheDocument();\n\n  // Assert persistence\n  expect(repo.data.transactions).toHaveLength(1);\n});\n```\n\n### Key Gotchas\n\n- **Auto-focus timer**: BudgetShell has an 80ms setTimeout to focus the amount input. In StrictMode this fires twice. Wait for focus before typing: `await waitFor(() => expect(input).toHaveFocus())`.\n- **Scoped queries**: Amounts, account names, etc. appear in multiple places (sidebar, table, net worth). Use `within(table)` or `within(dialog)` to scope assertions.\n- **Cleanup**: `renderApp` handles QueryClient cleanup between tests automatically. No manual teardown needed.\n\n## Commands\n\n```bash\nnpm test              # Run all tests (vitest run)\nnpm run test:watch    # Vitest in watch mode\n```\n\n## Linting\n\n**ESLint 9** (flat config) with TypeScript, React Hooks, and React Refresh rules. The `react-refresh/only-export-components` rule is disabled for route files and shadcn components since those patterns require multi-export files.\n",
})

export const SPEC_FILENAMES: readonly string[] = Object.freeze([
  "APP_KNOWLEDGE.md",
  "APP_MAP.md",
  "ARCHITECTURE.md",
  "DATA_MODEL.md",
  "FRONTEND_DESIGN.md",
  "I18N.md",
  "IMPORT.md",
  "INTELLIGENCE.md",
  "MONOREPO.md",
  "PRODUCT.md",
  "STRUCTURE.md",
  "TESTING.md",
])
