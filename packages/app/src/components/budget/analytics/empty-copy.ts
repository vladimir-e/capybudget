/** Shown across analytics tabs when the budget has no transactions at all — a
 *  brand-new budget, not just an empty period. Forward-looking and calm: no CTA,
 *  no exclamation. Period-scoped copy ("No expenses in this period") stays local
 *  to each tab. */
export const NO_DATA_YET = "This will be useful once you have more data.";
